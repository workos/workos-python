# This file is auto-generated by oagen. Do not edit.

import json

import pytest
from workos import WorkOS, AsyncWorkOS
from tests.generated_helpers import load_fixture

from workos.user_management.organization_membership.models import (
    OrganizationMembership,
    UserOrganizationMembership,
    UserManagementOrganizationMembershipOrder,
)
from workos._pagination import AsyncPage, SyncPage
from workos._errors import (
    AuthenticationException,
    NotFoundException,
    RateLimitExceededException,
    ServerException,
)


class TestUserManagementOrganizationMembership:
    def test_list(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("list_user_organization_membership.json"),
        )
        page = workos.user_management.organization_membership.list()
        assert isinstance(page, SyncPage)
        assert isinstance(page.data, list)

    def test_list_empty_page(self, workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = workos.user_management.organization_membership.list()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_encodes_query_params(self, workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        workos.user_management.organization_membership.list(
            limit=10,
            before="cursor before",
            after="cursor/after",
            order=UserManagementOrganizationMembershipOrder("normal"),
            organization_id="value organization_id/test",
            user_id="value user_id/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["order"] == "normal"
        assert request.url.params["organization_id"] == "value organization_id/test"
        assert request.url.params["user_id"] == "value user_id/test"

    def test_create(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("organization_membership.json"),
        )
        result = workos.user_management.organization_membership.create(
            user_id="test_user_id", organization_id="test_organization_id"
        )
        assert isinstance(result, OrganizationMembership)
        assert result.object == "organization_membership"
        assert result.id == "om_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/organization_memberships")
        body = json.loads(request.content)
        assert body["user_id"] == "test_user_id"
        assert body["organization_id"] == "test_organization_id"

    def test_get(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("user_organization_membership.json"),
        )
        result = workos.user_management.organization_membership.get("test_id")
        assert isinstance(result, UserOrganizationMembership)
        assert result.object == "organization_membership"
        assert result.id == "om_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/user_management/organization_memberships/test_id"
        )

    def test_update(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("user_organization_membership.json"),
        )
        result = workos.user_management.organization_membership.update("test_id")
        assert isinstance(result, UserOrganizationMembership)
        assert result.object == "organization_membership"
        assert result.id == "om_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/user_management/organization_memberships/test_id"
        )

    def test_delete(self, workos, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = workos.user_management.organization_membership.delete("test_id")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/user_management/organization_memberships/test_id"
        )

    def test_deactivate(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("organization_membership.json"),
        )
        result = workos.user_management.organization_membership.deactivate("test_id")
        assert isinstance(result, OrganizationMembership)
        assert result.object == "organization_membership"
        assert result.id == "om_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/user_management/organization_memberships/test_id/deactivate"
        )

    def test_reactivate(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("user_organization_membership.json"),
        )
        result = workos.user_management.organization_membership.reactivate("test_id")
        assert isinstance(result, UserOrganizationMembership)
        assert result.object == "organization_membership"
        assert result.id == "om_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/user_management/organization_memberships/test_id/reactivate"
        )

    def test_list_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationException):
            workos.user_management.organization_membership.list()

    def test_list_not_found(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                workos.user_management.organization_membership.list()
        finally:
            workos.close()

    def test_list_rate_limited(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                workos.user_management.organization_membership.list()
        finally:
            workos.close()

    def test_list_server_error(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                workos.user_management.organization_membership.list()
        finally:
            workos.close()


@pytest.mark.asyncio
class TestAsyncUserManagementOrganizationMembership:
    async def test_list(self, async_workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("list_user_organization_membership.json")
        )
        page = await async_workos.user_management.organization_membership.list()
        assert isinstance(page, AsyncPage)
        assert isinstance(page.data, list)

    async def test_list_empty_page(self, async_workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_workos.user_management.organization_membership.list()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    async def test_list_encodes_query_params(self, async_workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_workos.user_management.organization_membership.list(
            limit=10,
            before="cursor before",
            after="cursor/after",
            order=UserManagementOrganizationMembershipOrder("normal"),
            organization_id="value organization_id/test",
            user_id="value user_id/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["order"] == "normal"
        assert request.url.params["organization_id"] == "value organization_id/test"
        assert request.url.params["user_id"] == "value user_id/test"

    async def test_create(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("organization_membership.json"))
        result = await async_workos.user_management.organization_membership.create(
            user_id="test_user_id", organization_id="test_organization_id"
        )
        assert isinstance(result, OrganizationMembership)
        assert result.object == "organization_membership"
        assert result.id == "om_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/organization_memberships")

    async def test_get(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("user_organization_membership.json"))
        result = await async_workos.user_management.organization_membership.get(
            "test_id"
        )
        assert isinstance(result, UserOrganizationMembership)
        assert result.object == "organization_membership"
        assert result.id == "om_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/user_management/organization_memberships/test_id"
        )

    async def test_update(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("user_organization_membership.json"))
        result = await async_workos.user_management.organization_membership.update(
            "test_id"
        )
        assert isinstance(result, UserOrganizationMembership)
        assert result.object == "organization_membership"
        assert result.id == "om_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/user_management/organization_memberships/test_id"
        )

    async def test_delete(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = await async_workos.user_management.organization_membership.delete(
            "test_id"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/user_management/organization_memberships/test_id"
        )

    async def test_deactivate(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("organization_membership.json"))
        result = await async_workos.user_management.organization_membership.deactivate(
            "test_id"
        )
        assert isinstance(result, OrganizationMembership)
        assert result.object == "organization_membership"
        assert result.id == "om_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/user_management/organization_memberships/test_id/deactivate"
        )

    async def test_reactivate(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("user_organization_membership.json"))
        result = await async_workos.user_management.organization_membership.reactivate(
            "test_id"
        )
        assert isinstance(result, UserOrganizationMembership)
        assert result.object == "organization_membership"
        assert result.id == "om_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/user_management/organization_memberships/test_id/reactivate"
        )

    async def test_list_unauthorized(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationException):
            await async_workos.user_management.organization_membership.list()

    async def test_list_not_found(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                await workos.user_management.organization_membership.list()
        finally:
            await workos.close()

    async def test_list_rate_limited(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                await workos.user_management.organization_membership.list()
        finally:
            await workos.close()

    async def test_list_server_error(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                await workos.user_management.organization_membership.list()
        finally:
            await workos.close()
