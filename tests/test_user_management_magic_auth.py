# This file is auto-generated by oagen. Do not edit.

import json

import pytest
from workos import WorkOS, AsyncWorkOS
from tests.generated_helpers import load_fixture

from workos.user_management.magic_auth.models import MagicAuth
from workos._errors import (
    AuthenticationException,
    NotFoundException,
    RateLimitExceededException,
    ServerException,
)


class TestUserManagementMagicAuth:
    def test_send_magic_auth_code_and_return(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("magic_auth.json"),
        )
        result = workos.user_management.magic_auth.send_magic_auth_code_and_return(
            email="test_email"
        )
        assert isinstance(result, MagicAuth)
        assert result.object == "magic_auth"
        assert result.id == "magic_auth_01HWZBQZY2M3AMQW166Q22K88F"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/magic_auth")
        body = json.loads(request.content)
        assert body["email"] == "test_email"

    def test_get(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("magic_auth.json"),
        )
        result = workos.user_management.magic_auth.get("test_id")
        assert isinstance(result, MagicAuth)
        assert result.object == "magic_auth"
        assert result.id == "magic_auth_01HWZBQZY2M3AMQW166Q22K88F"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/user_management/magic_auth/test_id")

    def test_send_magic_auth_code_and_return_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationException):
            workos.user_management.magic_auth.send_magic_auth_code_and_return(
                email="test_email"
            )

    def test_send_magic_auth_code_and_return_not_found(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                workos.user_management.magic_auth.send_magic_auth_code_and_return(
                    email="test_email"
                )
        finally:
            workos.close()

    def test_send_magic_auth_code_and_return_rate_limited(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                workos.user_management.magic_auth.send_magic_auth_code_and_return(
                    email="test_email"
                )
        finally:
            workos.close()

    def test_send_magic_auth_code_and_return_server_error(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                workos.user_management.magic_auth.send_magic_auth_code_and_return(
                    email="test_email"
                )
        finally:
            workos.close()


@pytest.mark.asyncio
class TestAsyncUserManagementMagicAuth:
    async def test_send_magic_auth_code_and_return(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("magic_auth.json"))
        result = await async_workos.user_management.magic_auth.send_magic_auth_code_and_return(
            email="test_email"
        )
        assert isinstance(result, MagicAuth)
        assert result.object == "magic_auth"
        assert result.id == "magic_auth_01HWZBQZY2M3AMQW166Q22K88F"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/magic_auth")

    async def test_get(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("magic_auth.json"))
        result = await async_workos.user_management.magic_auth.get("test_id")
        assert isinstance(result, MagicAuth)
        assert result.object == "magic_auth"
        assert result.id == "magic_auth_01HWZBQZY2M3AMQW166Q22K88F"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/user_management/magic_auth/test_id")

    async def test_send_magic_auth_code_and_return_unauthorized(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationException):
            await (
                async_workos.user_management.magic_auth.send_magic_auth_code_and_return(
                    email="test_email"
                )
            )

    async def test_send_magic_auth_code_and_return_not_found(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                await workos.user_management.magic_auth.send_magic_auth_code_and_return(
                    email="test_email"
                )
        finally:
            await workos.close()

    async def test_send_magic_auth_code_and_return_rate_limited(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                await workos.user_management.magic_auth.send_magic_auth_code_and_return(
                    email="test_email"
                )
        finally:
            await workos.close()

    async def test_send_magic_auth_code_and_return_server_error(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                await workos.user_management.magic_auth.send_magic_auth_code_and_return(
                    email="test_email"
                )
        finally:
            await workos.close()
