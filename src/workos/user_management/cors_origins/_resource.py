# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, Optional

if TYPE_CHECKING:
    from ..._client import AsyncWorkOSClient, WorkOSClient

from ..._types import RequestOptions
from .models import CORSOriginResponse


class UserManagementCorsOrigins:
    """User Management Cors Origins API resources."""

    def __init__(self, client: "WorkOSClient") -> None:
        self._client = client

    def create_cors_origin(
        self,
        *,
        origin: str,
        request_options: Optional[RequestOptions] = None,
    ) -> CORSOriginResponse:
        """Create a CORS origin

        Creates a new CORS origin for the current environment. CORS origins allow browser-based applications to make requests to the WorkOS API.

                Args:
                    origin: The origin URL to allow for CORS requests.
                    request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

                Returns:
                    CORSOriginResponse

                Raises:
                    ConflictException: If a conflict occurs (409).
                    UnprocessableEntityException: If the request data is unprocessable (422).
                    AuthenticationException: If the API key is invalid (401).
                    RateLimitExceededException: If rate limited (429).
                    ServerException: If the server returns a 5xx error.
        """
        body: Dict[str, Any] = {
            "origin": origin,
        }
        return self._client.request(
            method="post",
            path="user_management/cors_origins",
            body=body,
            model=CORSOriginResponse,
            request_options=request_options,
        )


class AsyncUserManagementCorsOrigins:
    """User Management Cors Origins API resources (async)."""

    def __init__(self, client: "AsyncWorkOSClient") -> None:
        self._client = client

    async def create_cors_origin(
        self,
        *,
        origin: str,
        request_options: Optional[RequestOptions] = None,
    ) -> CORSOriginResponse:
        """Create a CORS origin

        Creates a new CORS origin for the current environment. CORS origins allow browser-based applications to make requests to the WorkOS API.

                Args:
                    origin: The origin URL to allow for CORS requests.
                    request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

                Returns:
                    CORSOriginResponse

                Raises:
                    ConflictException: If a conflict occurs (409).
                    UnprocessableEntityException: If the request data is unprocessable (422).
                    AuthenticationException: If the API key is invalid (401).
                    RateLimitExceededException: If rate limited (429).
                    ServerException: If the server returns a 5xx error.
        """
        body: Dict[str, Any] = {
            "origin": origin,
        }
        return await self._client.request(
            method="post",
            path="user_management/cors_origins",
            body=body,
            model=CORSOriginResponse,
            request_options=request_options,
        )
