# This file is auto-generated by oagen. Do not edit.

import json

import pytest
from workos import WorkOSClient, AsyncWorkOSClient
from tests.generated_helpers import load_fixture

from workos.common.models import PaginationOrder
from workos.flag_target_types.models import FlagTargetType
from workos._pagination import AsyncPage, SyncPage
from workos._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)


class TestFlagTargetTypes:
    def test_list_flag_target_types(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("list_flag_target_type.json"),
        )
        page = workos.flag_target_types.list_flag_target_types()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(page.data[0], FlagTargetType)

    def test_list_flag_target_types_empty_page(self, workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = workos.flag_target_types.list_flag_target_types()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_flag_target_types_encodes_query_params(self, workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        workos.flag_target_types.list_flag_target_types(
            limit=10,
            before="cursor before",
            after="cursor/after",
            order=PaginationOrder("value_order"),
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["order"] == "value_order"

    def test_create_flag_target_type(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("flag_target_type.json"),
        )
        result = workos.flag_target_types.create_flag_target_type(slug="test_slug")
        assert isinstance(result, FlagTargetType)
        assert result.object == "flag_target_type"
        assert result.id == "flag_target_type_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/flag_target_types")
        body = json.loads(request.content)
        assert body["slug"] == "test_slug"

    def test_get_flag_target_type(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("flag_target_type.json"),
        )
        result = workos.flag_target_types.get_flag_target_type("test_id")
        assert isinstance(result, FlagTargetType)
        assert result.object == "flag_target_type"
        assert result.id == "flag_target_type_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/flag_target_types/test_id")

    def test_list_flag_target_types_with_request_options(self, workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        workos.flag_target_types.list_flag_target_types(
            request_options={"extra_headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_flag_target_types_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            workos.flag_target_types.list_flag_target_types()

    def test_list_flag_target_types_not_found(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                workos.flag_target_types.list_flag_target_types()
        finally:
            workos.close()

    def test_list_flag_target_types_rate_limited(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                workos.flag_target_types.list_flag_target_types()
        finally:
            workos.close()

    def test_list_flag_target_types_server_error(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                workos.flag_target_types.list_flag_target_types()
        finally:
            workos.close()

    def test_list_flag_target_types_bad_request(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                workos.flag_target_types.list_flag_target_types()
        finally:
            workos.close()

    def test_list_flag_target_types_unprocessable(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                workos.flag_target_types.list_flag_target_types()
        finally:
            workos.close()


class TestAsyncFlagTargetTypes:
    @pytest.mark.asyncio
    async def test_list_flag_target_types(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("list_flag_target_type.json"))
        page = await async_workos.flag_target_types.list_flag_target_types()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(page.data[0], FlagTargetType)

    @pytest.mark.asyncio
    async def test_list_flag_target_types_empty_page(self, async_workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_workos.flag_target_types.list_flag_target_types()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_list_flag_target_types_encodes_query_params(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_workos.flag_target_types.list_flag_target_types(
            limit=10,
            before="cursor before",
            after="cursor/after",
            order=PaginationOrder("value_order"),
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["order"] == "value_order"

    @pytest.mark.asyncio
    async def test_create_flag_target_type(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("flag_target_type.json"))
        result = await async_workos.flag_target_types.create_flag_target_type(
            slug="test_slug"
        )
        assert isinstance(result, FlagTargetType)
        assert result.object == "flag_target_type"
        assert result.id == "flag_target_type_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/flag_target_types")

    @pytest.mark.asyncio
    async def test_get_flag_target_type(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("flag_target_type.json"))
        result = await async_workos.flag_target_types.get_flag_target_type("test_id")
        assert isinstance(result, FlagTargetType)
        assert result.object == "flag_target_type"
        assert result.id == "flag_target_type_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/flag_target_types/test_id")

    @pytest.mark.asyncio
    async def test_list_flag_target_types_with_request_options(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_workos.flag_target_types.list_flag_target_types(
            request_options={"extra_headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_flag_target_types_unauthorized(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_workos.flag_target_types.list_flag_target_types()

    @pytest.mark.asyncio
    async def test_list_flag_target_types_not_found(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await workos.flag_target_types.list_flag_target_types()
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_list_flag_target_types_rate_limited(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await workos.flag_target_types.list_flag_target_types()
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_list_flag_target_types_server_error(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await workos.flag_target_types.list_flag_target_types()
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_list_flag_target_types_bad_request(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await workos.flag_target_types.list_flag_target_types()
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_list_flag_target_types_unprocessable(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await workos.flag_target_types.list_flag_target_types()
        finally:
            await workos.close()
