# This file is auto-generated by oagen. Do not edit.

import json

import pytest
from workos import WorkOS, AsyncWorkOS
from tests.generated_helpers import load_fixture

from workos.user_management.invitations.models import (
    Invitation,
    UserInvite,
    UserManagementInvitationsOrder,
)
from workos._pagination import AsyncPage, SyncPage
from workos._errors import (
    AuthenticationException,
    NotFoundException,
    RateLimitExceededException,
    ServerException,
)


class TestUserManagementInvitations:
    def test_list(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("list_user_invite.json"),
        )
        page = workos.user_management.invitations.list()
        assert isinstance(page, SyncPage)
        assert isinstance(page.data, list)

    def test_list_empty_page(self, workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = workos.user_management.invitations.list()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_encodes_query_params(self, workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        workos.user_management.invitations.list(
            limit=10,
            before="cursor before",
            after="cursor/after",
            order=UserManagementInvitationsOrder("normal"),
            organization_id="value organization_id/test",
            email="value email/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["order"] == "normal"
        assert request.url.params["organization_id"] == "value organization_id/test"
        assert request.url.params["email"] == "value email/test"

    def test_create(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("user_invite.json"),
        )
        result = workos.user_management.invitations.create(email="test_email")
        assert isinstance(result, UserInvite)
        assert result.object == "invitation"
        assert result.id == "invitation_01E4ZCR3C56J083X43JQXF3JK5"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/invitations")
        body = json.loads(request.content)
        assert body["email"] == "test_email"

    def test_get_by_token(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("user_invite.json"),
        )
        result = workos.user_management.invitations.get_by_token("test_token")
        assert isinstance(result, UserInvite)
        assert result.object == "invitation"
        assert result.id == "invitation_01E4ZCR3C56J083X43JQXF3JK5"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/user_management/invitations/by_token/test_token"
        )

    def test_get(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("user_invite.json"),
        )
        result = workos.user_management.invitations.get("test_id")
        assert isinstance(result, UserInvite)
        assert result.object == "invitation"
        assert result.id == "invitation_01E4ZCR3C56J083X43JQXF3JK5"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/user_management/invitations/test_id")

    def test_accept(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("invitation.json"),
        )
        result = workos.user_management.invitations.accept("test_id")
        assert isinstance(result, Invitation)
        assert result.object == "invitation"
        assert result.id == "invitation_01E4ZCR3C56J083X43JQXF3JK5"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/invitations/test_id/accept")

    def test_resend(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("user_invite.json"),
        )
        result = workos.user_management.invitations.resend("test_id")
        assert isinstance(result, UserInvite)
        assert result.object == "invitation"
        assert result.id == "invitation_01E4ZCR3C56J083X43JQXF3JK5"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/invitations/test_id/resend")

    def test_revoke(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("invitation.json"),
        )
        result = workos.user_management.invitations.revoke("test_id")
        assert isinstance(result, Invitation)
        assert result.object == "invitation"
        assert result.id == "invitation_01E4ZCR3C56J083X43JQXF3JK5"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/invitations/test_id/revoke")

    def test_list_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationException):
            workos.user_management.invitations.list()

    def test_list_not_found(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                workos.user_management.invitations.list()
        finally:
            workos.close()

    def test_list_rate_limited(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                workos.user_management.invitations.list()
        finally:
            workos.close()

    def test_list_server_error(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                workos.user_management.invitations.list()
        finally:
            workos.close()


@pytest.mark.asyncio
class TestAsyncUserManagementInvitations:
    async def test_list(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("list_user_invite.json"))
        page = await async_workos.user_management.invitations.list()
        assert isinstance(page, AsyncPage)
        assert isinstance(page.data, list)

    async def test_list_empty_page(self, async_workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_workos.user_management.invitations.list()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    async def test_list_encodes_query_params(self, async_workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_workos.user_management.invitations.list(
            limit=10,
            before="cursor before",
            after="cursor/after",
            order=UserManagementInvitationsOrder("normal"),
            organization_id="value organization_id/test",
            email="value email/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["order"] == "normal"
        assert request.url.params["organization_id"] == "value organization_id/test"
        assert request.url.params["email"] == "value email/test"

    async def test_create(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("user_invite.json"))
        result = await async_workos.user_management.invitations.create(
            email="test_email"
        )
        assert isinstance(result, UserInvite)
        assert result.object == "invitation"
        assert result.id == "invitation_01E4ZCR3C56J083X43JQXF3JK5"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/invitations")

    async def test_get_by_token(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("user_invite.json"))
        result = await async_workos.user_management.invitations.get_by_token(
            "test_token"
        )
        assert isinstance(result, UserInvite)
        assert result.object == "invitation"
        assert result.id == "invitation_01E4ZCR3C56J083X43JQXF3JK5"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/user_management/invitations/by_token/test_token"
        )

    async def test_get(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("user_invite.json"))
        result = await async_workos.user_management.invitations.get("test_id")
        assert isinstance(result, UserInvite)
        assert result.object == "invitation"
        assert result.id == "invitation_01E4ZCR3C56J083X43JQXF3JK5"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/user_management/invitations/test_id")

    async def test_accept(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("invitation.json"))
        result = await async_workos.user_management.invitations.accept("test_id")
        assert isinstance(result, Invitation)
        assert result.object == "invitation"
        assert result.id == "invitation_01E4ZCR3C56J083X43JQXF3JK5"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/invitations/test_id/accept")

    async def test_resend(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("user_invite.json"))
        result = await async_workos.user_management.invitations.resend("test_id")
        assert isinstance(result, UserInvite)
        assert result.object == "invitation"
        assert result.id == "invitation_01E4ZCR3C56J083X43JQXF3JK5"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/invitations/test_id/resend")

    async def test_revoke(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("invitation.json"))
        result = await async_workos.user_management.invitations.revoke("test_id")
        assert isinstance(result, Invitation)
        assert result.object == "invitation"
        assert result.id == "invitation_01E4ZCR3C56J083X43JQXF3JK5"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/invitations/test_id/revoke")

    async def test_list_unauthorized(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationException):
            await async_workos.user_management.invitations.list()

    async def test_list_not_found(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                await workos.user_management.invitations.list()
        finally:
            await workos.close()

    async def test_list_rate_limited(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                await workos.user_management.invitations.list()
        finally:
            await workos.close()

    async def test_list_server_error(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                await workos.user_management.invitations.list()
        finally:
            await workos.close()
