# This file is auto-generated by oagen. Do not edit.


import pytest
from workos import WorkOS, AsyncWorkOS

from workos._errors import (
    AuthenticationException,
    NotFoundException,
    RateLimitExceededException,
    ServerException,
)


class TestFeatureFlagsTargets:
    def test_create_target(self, workos, httpx_mock):
        httpx_mock.add_response(json={})
        workos.feature_flags.targets.create_target("test_resourceId", "test_slug")
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/feature-flags/test_slug/targets/test_resourceId"
        )

    def test_delete_target(self, workos, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = workos.feature_flags.targets.delete_target(
            "test_resourceId", "test_slug"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/feature-flags/test_slug/targets/test_resourceId"
        )

    def test_create_target_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationException):
            workos.feature_flags.targets.create_target("test_resourceId", "test_slug")

    def test_create_target_not_found(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                workos.feature_flags.targets.create_target(
                    "test_resourceId", "test_slug"
                )
        finally:
            workos.close()

    def test_create_target_rate_limited(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                workos.feature_flags.targets.create_target(
                    "test_resourceId", "test_slug"
                )
        finally:
            workos.close()

    def test_create_target_server_error(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                workos.feature_flags.targets.create_target(
                    "test_resourceId", "test_slug"
                )
        finally:
            workos.close()


@pytest.mark.asyncio
class TestAsyncFeatureFlagsTargets:
    async def test_create_target(self, async_workos, httpx_mock):
        httpx_mock.add_response(json={})
        await async_workos.feature_flags.targets.create_target(
            "test_resourceId", "test_slug"
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/feature-flags/test_slug/targets/test_resourceId"
        )

    async def test_delete_target(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = await async_workos.feature_flags.targets.delete_target(
            "test_resourceId", "test_slug"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/feature-flags/test_slug/targets/test_resourceId"
        )

    async def test_create_target_unauthorized(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationException):
            await async_workos.feature_flags.targets.create_target(
                "test_resourceId", "test_slug"
            )

    async def test_create_target_not_found(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                await workos.feature_flags.targets.create_target(
                    "test_resourceId", "test_slug"
                )
        finally:
            await workos.close()

    async def test_create_target_rate_limited(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                await workos.feature_flags.targets.create_target(
                    "test_resourceId", "test_slug"
                )
        finally:
            await workos.close()

    async def test_create_target_server_error(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                await workos.feature_flags.targets.create_target(
                    "test_resourceId", "test_slug"
                )
        finally:
            await workos.close()
