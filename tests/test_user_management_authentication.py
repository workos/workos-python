# This file is auto-generated by oagen. Do not edit.

import json

import pytest
from workos import WorkOS, AsyncWorkOS
from tests.generated_helpers import load_fixture

from workos.user_management.authentication.models import (
    AuthenticateResponse,
    DeviceAuthorizationResponse,
)
from workos._errors import (
    AuthenticationException,
    NotFoundException,
    RateLimitExceededException,
    ServerException,
)


class TestUserManagementAuthentication:
    def test_authenticate(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("authenticate_response.json"),
        )
        result = workos.user_management.authentication.authenticate(
            body=load_fixture("authorization_code_session_authenticate_request.json")
        )
        assert isinstance(result, AuthenticateResponse)
        assert result.organization_id == "org_01H945H0YD4F97JN9MATX7BYAG"
        assert result.authkit_authorization_code == "authkit_authz_code_abc123"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/authenticate")

    def test_authorize(self, workos):
        result = workos.user_management.authentication.authorize(
            response_type="code",
            redirect_uri="test_redirect_uri",
            client_id="test_client_id",
        )
        assert isinstance(result, str)
        assert result.startswith("http")

    def test_device_authorization(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("device_authorization_response.json"),
        )
        result = workos.user_management.authentication.device_authorization(
            client_id="test_client_id"
        )
        assert isinstance(result, DeviceAuthorizationResponse)
        assert (
            result.device_code
            == "CVE2wOfIFK4vhmiDBntpX9s8KT2f0qngpWYL0LGy9HxYgBRXUKIUkZB9BgIFho5h"
        )
        assert result.user_code == "BCDF-GHJK"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/authorize/device")
        body = json.loads(request.content)
        assert body["client_id"] == "test_client_id"

    def test_logout(self, workos):
        result = workos.user_management.authentication.logout(
            session_id="test_session_id"
        )
        assert isinstance(result, str)
        assert result.startswith("http")

    def test_revoke_session(self, workos, httpx_mock):
        httpx_mock.add_response(json={})
        workos.user_management.authentication.revoke_session(
            session_id="test_session_id"
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/sessions/revoke")

    def test_authenticate_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationException):
            workos.user_management.authentication.authenticate(
                body=load_fixture(
                    "authorization_code_session_authenticate_request.json"
                )
            )

    def test_authenticate_not_found(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                workos.user_management.authentication.authenticate(
                    body=load_fixture(
                        "authorization_code_session_authenticate_request.json"
                    )
                )
        finally:
            workos.close()

    def test_authenticate_rate_limited(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                workos.user_management.authentication.authenticate(
                    body=load_fixture(
                        "authorization_code_session_authenticate_request.json"
                    )
                )
        finally:
            workos.close()

    def test_authenticate_server_error(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                workos.user_management.authentication.authenticate(
                    body=load_fixture(
                        "authorization_code_session_authenticate_request.json"
                    )
                )
        finally:
            workos.close()


@pytest.mark.asyncio
class TestAsyncUserManagementAuthentication:
    async def test_authenticate(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("authenticate_response.json"))
        result = await async_workos.user_management.authentication.authenticate(
            body=load_fixture("authorization_code_session_authenticate_request.json")
        )
        assert isinstance(result, AuthenticateResponse)
        assert result.organization_id == "org_01H945H0YD4F97JN9MATX7BYAG"
        assert result.authkit_authorization_code == "authkit_authz_code_abc123"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/authenticate")

    async def test_authorize(self, async_workos):
        result = await async_workos.user_management.authentication.authorize(
            response_type="code",
            redirect_uri="test_redirect_uri",
            client_id="test_client_id",
        )
        assert isinstance(result, str)
        assert result.startswith("http")

    async def test_device_authorization(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("device_authorization_response.json"))
        result = await async_workos.user_management.authentication.device_authorization(
            client_id="test_client_id"
        )
        assert isinstance(result, DeviceAuthorizationResponse)
        assert (
            result.device_code
            == "CVE2wOfIFK4vhmiDBntpX9s8KT2f0qngpWYL0LGy9HxYgBRXUKIUkZB9BgIFho5h"
        )
        assert result.user_code == "BCDF-GHJK"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/authorize/device")

    async def test_logout(self, async_workos):
        result = await async_workos.user_management.authentication.logout(
            session_id="test_session_id"
        )
        assert isinstance(result, str)
        assert result.startswith("http")

    async def test_revoke_session(self, async_workos, httpx_mock):
        httpx_mock.add_response(json={})
        await async_workos.user_management.authentication.revoke_session(
            session_id="test_session_id"
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/sessions/revoke")

    async def test_authenticate_unauthorized(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationException):
            await async_workos.user_management.authentication.authenticate(
                body=load_fixture(
                    "authorization_code_session_authenticate_request.json"
                )
            )

    async def test_authenticate_not_found(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                await workos.user_management.authentication.authenticate(
                    body=load_fixture(
                        "authorization_code_session_authenticate_request.json"
                    )
                )
        finally:
            await workos.close()

    async def test_authenticate_rate_limited(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                await workos.user_management.authentication.authenticate(
                    body=load_fixture(
                        "authorization_code_session_authenticate_request.json"
                    )
                )
        finally:
            await workos.close()

    async def test_authenticate_server_error(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                await workos.user_management.authentication.authenticate(
                    body=load_fixture(
                        "authorization_code_session_authenticate_request.json"
                    )
                )
        finally:
            await workos.close()
