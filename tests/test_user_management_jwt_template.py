# This file is auto-generated by oagen. Do not edit.

import json

import pytest
from workos import WorkOS, AsyncWorkOS
from tests.generated_helpers import load_fixture

from workos.user_management.jwt_template.models import JWTTemplateResponse
from workos._errors import (
    AuthenticationException,
    NotFoundException,
    RateLimitExceededException,
    ServerException,
)


class TestUserManagementJWTTemplate:
    def test_update_jwt_template(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("jwt_template_response.json"),
        )
        result = workos.user_management.jwt_template.update_jwt_template(
            content="test_content"
        )
        assert isinstance(result, JWTTemplateResponse)
        assert result.object == "jwt_template"
        assert result.created_at == "2026-01-15T12:00:00.000Z"
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/user_management/jwt_template")
        body = json.loads(request.content)
        assert body["content"] == "test_content"

    def test_update_jwt_template_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationException):
            workos.user_management.jwt_template.update_jwt_template(
                content="test_content"
            )

    def test_update_jwt_template_not_found(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                workos.user_management.jwt_template.update_jwt_template(
                    content="test_content"
                )
        finally:
            workos.close()

    def test_update_jwt_template_rate_limited(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                workos.user_management.jwt_template.update_jwt_template(
                    content="test_content"
                )
        finally:
            workos.close()

    def test_update_jwt_template_server_error(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                workos.user_management.jwt_template.update_jwt_template(
                    content="test_content"
                )
        finally:
            workos.close()


@pytest.mark.asyncio
class TestAsyncUserManagementJWTTemplate:
    async def test_update_jwt_template(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("jwt_template_response.json"))
        result = await async_workos.user_management.jwt_template.update_jwt_template(
            content="test_content"
        )
        assert isinstance(result, JWTTemplateResponse)
        assert result.object == "jwt_template"
        assert result.created_at == "2026-01-15T12:00:00.000Z"
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith("/user_management/jwt_template")

    async def test_update_jwt_template_unauthorized(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationException):
            await async_workos.user_management.jwt_template.update_jwt_template(
                content="test_content"
            )

    async def test_update_jwt_template_not_found(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                await workos.user_management.jwt_template.update_jwt_template(
                    content="test_content"
                )
        finally:
            await workos.close()

    async def test_update_jwt_template_rate_limited(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                await workos.user_management.jwt_template.update_jwt_template(
                    content="test_content"
                )
        finally:
            await workos.close()

    async def test_update_jwt_template_server_error(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                await workos.user_management.jwt_template.update_jwt_template(
                    content="test_content"
                )
        finally:
            await workos.close()
