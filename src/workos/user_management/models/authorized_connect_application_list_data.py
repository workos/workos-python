# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from typing import cast
from typing import Any, Dict, List, Literal
from workos._errors import WorkOSError

from workos.connect.models import ConnectApplication


@dataclass(slots=True)
class AuthorizedConnectApplicationListData:
    """Authorized Connect Application List Data model."""

    object: Literal["authorized_connect_application"]
    """Distinguishes the authorized connect application object."""
    id: str
    """The unique ID of the authorized connect application."""
    granted_scopes: List[str]
    """The scopes granted by the user to the application."""
    application: "ConnectApplication"

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> "AuthorizedConnectApplicationListData":
        """Deserialize from a dictionary."""
        try:
            return cls(
                object=data["object"],
                id=data["id"],
                granted_scopes=data["granted_scopes"],
                application=ConnectApplication.from_dict(
                    cast(Dict[str, Any], data["application"])
                ),
            )
        except (KeyError, ValueError) as e:
            raise WorkOSError(
                f"Unexpected API response while parsing AuthorizedConnectApplicationListData: {e!s}"
            ) from e

    def to_dict(self) -> Dict[str, Any]:
        """Serialize to a dictionary."""
        result: Dict[str, Any] = {}
        result["object"] = self.object
        result["id"] = self.id
        result["granted_scopes"] = self.granted_scopes
        result["application"] = self.application.to_dict()
        return result
