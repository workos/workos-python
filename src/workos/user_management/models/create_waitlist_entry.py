# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from typing import Any

from workos._types import _raise_deserialize_error


@dataclass(slots=True)
class CreateWaitlistEntry:
    """Create Waitlist Entry model."""

    email: str
    """The email address of the user joining the waitlist."""
    additional_fields: dict[str, str] | None = None
    """Object containing additional key/value pairs collected with the waitlist entry. Supports up to 50 string pairs, with keys up to 40 characters and values up to 600 characters. Values are user-provided — treat them as untrusted input when rendering or exporting."""
    send_confirmation_email: bool | None = None
    """Whether to send the waitlist confirmation email to the user. Defaults to `false`. No email is sent when the waitlist confirmation email is disabled in the environment, even if `send_confirmation_email` is `true`."""

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> CreateWaitlistEntry:
        """Deserialize from a dictionary."""
        try:
            return cls(
                email=data["email"],
                additional_fields=data.get("additional_fields"),
                send_confirmation_email=data.get("send_confirmation_email"),
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error("CreateWaitlistEntry", e)

    def to_dict(self) -> dict[str, Any]:
        """Serialize to a dictionary."""
        result: dict[str, Any] = {}
        result["email"] = self.email
        if self.additional_fields is not None:
            result["additional_fields"] = self.additional_fields
        if self.send_confirmation_email is not None:
            result["send_confirmation_email"] = self.send_confirmation_email
        return result
