# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, Optional, Union

if TYPE_CHECKING:
    from .._client import AsyncWorkOSClient, WorkOSClient

from .._types import RequestOptions, enum_value
from .models import FlagTargetType
from workos.common.models.pagination_order import PaginationOrder
from .._pagination import AsyncPage, SyncPage


class FlagTargetTypes:
    """Flag Target Types API resources."""

    def __init__(self, client: "WorkOSClient") -> None:
        self._client = client

    def list_flag_target_types(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[Union[PaginationOrder, str]] = "desc",
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[FlagTargetType]:
        """List flag target types

        Get a list of the custom Flag Target Types registered for the current environment's project. The built-in `user` and `organization` target types are not included.

        Args:
            limit: Upper limit on the number of objects to return, between `1` and `100`. Defaults to `10`.
            before: An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
            after: An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
            order: Order the results by the creation time. Defaults to `desc`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[FlagTargetType]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": enum_value(order) if order is not None else None,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("flag_target_types",),
            model=FlagTargetType,
            params=params,
            request_options=request_options,
        )

    def create_flag_target_type(
        self,
        *,
        slug: str,
        request_options: Optional[RequestOptions] = None,
    ) -> FlagTargetType:
        """Create a flag target type

        Register a custom Flag Target Type, making its slug usable as the `target_type` of a Flag Target anywhere in the project.

        Args:
            slug: A unique key used as the `target_type` of a Flag Target, scoped to the project. May contain lowercase letters, numbers, underscores and hyphens.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            FlagTargetType

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        body: Dict[str, Any] = {
            "slug": slug,
        }
        return self._client.request(
            method="post",
            path=("flag_target_types",),
            body=body,
            model=FlagTargetType,
            request_options=request_options,
        )

    def get_flag_target_type(
        self,
        id: str,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> FlagTargetType:
        """Get a flag target type

        Get the details of an existing Flag Target Type.

        Args:
            id: Unique identifier of the Flag Target Type.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            FlagTargetType

        Raises:
            NotFoundError: If the resource is not found (404).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        return self._client.request(
            method="get",
            path=("flag_target_types", str(id)),
            model=FlagTargetType,
            request_options=request_options,
        )


class AsyncFlagTargetTypes:
    """Flag Target Types API resources (async)."""

    def __init__(self, client: "AsyncWorkOSClient") -> None:
        self._client = client

    async def list_flag_target_types(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[Union[PaginationOrder, str]] = "desc",
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[FlagTargetType]:
        """List flag target types

        Get a list of the custom Flag Target Types registered for the current environment's project. The built-in `user` and `organization` target types are not included.

        Args:
            limit: Upper limit on the number of objects to return, between `1` and `100`. Defaults to `10`.
            before: An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
            after: An object ID that defines your place in the list. When the ID is not present, you are at the end of the list.
            order: Order the results by the creation time. Defaults to `desc`.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[FlagTargetType]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": enum_value(order) if order is not None else None,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("flag_target_types",),
            model=FlagTargetType,
            params=params,
            request_options=request_options,
        )

    async def create_flag_target_type(
        self,
        *,
        slug: str,
        request_options: Optional[RequestOptions] = None,
    ) -> FlagTargetType:
        """Create a flag target type

        Register a custom Flag Target Type, making its slug usable as the `target_type` of a Flag Target anywhere in the project.

        Args:
            slug: A unique key used as the `target_type` of a Flag Target, scoped to the project. May contain lowercase letters, numbers, underscores and hyphens.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            FlagTargetType

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        body: Dict[str, Any] = {
            "slug": slug,
        }
        return await self._client.request(
            method="post",
            path=("flag_target_types",),
            body=body,
            model=FlagTargetType,
            request_options=request_options,
        )

    async def get_flag_target_type(
        self,
        id: str,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> FlagTargetType:
        """Get a flag target type

        Get the details of an existing Flag Target Type.

        Args:
            id: Unique identifier of the Flag Target Type.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            FlagTargetType

        Raises:
            NotFoundError: If the resource is not found (404).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        return await self._client.request(
            method="get",
            path=("flag_target_types", str(id)),
            model=FlagTargetType,
            request_options=request_options,
        )
