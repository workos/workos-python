# This file is auto-generated by oagen. Do not edit.

import json

import pytest
from workos import WorkOS, AsyncWorkOS
from tests.generated_helpers import load_fixture

from workos.workos_connect.models import ExternalAuthCompleteResponse, UserObject
from workos._errors import (
    AuthenticationException,
    NotFoundException,
    RateLimitExceededException,
    ServerException,
)


class TestWorkosConnect:
    def test_complete_login(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("external_auth_complete_response.json"),
        )
        result = workos.workos_connect.complete_login(
            external_auth_id="test_external_auth_id",
            user=UserObject.from_dict(load_fixture("user_object.json")),
        )
        assert isinstance(result, ExternalAuthCompleteResponse)
        assert (
            result.redirect_uri
            == "https://your-authkit-domain.workos.com/oauth/authorize/complete?state=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdGF0ZSI6InJhbmRvbV9zdGF0ZV9zdHJpbmciLCJpYXQiOjE3NDI2MDQ4NTN9.abc123def456ghi789"
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/authkit/oauth2/complete")
        body = json.loads(request.content)
        assert body["external_auth_id"] == "test_external_auth_id"
        assert "user" in body

    def test_complete_login_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationException):
            workos.workos_connect.complete_login(
                external_auth_id="test_external_auth_id",
                user=UserObject.from_dict(load_fixture("user_object.json")),
            )

    def test_complete_login_not_found(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                workos.workos_connect.complete_login(
                    external_auth_id="test_external_auth_id",
                    user=UserObject.from_dict(load_fixture("user_object.json")),
                )
        finally:
            workos.close()

    def test_complete_login_rate_limited(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                workos.workos_connect.complete_login(
                    external_auth_id="test_external_auth_id",
                    user=UserObject.from_dict(load_fixture("user_object.json")),
                )
        finally:
            workos.close()

    def test_complete_login_server_error(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                workos.workos_connect.complete_login(
                    external_auth_id="test_external_auth_id",
                    user=UserObject.from_dict(load_fixture("user_object.json")),
                )
        finally:
            workos.close()


@pytest.mark.asyncio
class TestAsyncWorkosConnect:
    async def test_complete_login(self, async_workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("external_auth_complete_response.json")
        )
        result = await async_workos.workos_connect.complete_login(
            external_auth_id="test_external_auth_id",
            user=UserObject.from_dict(load_fixture("user_object.json")),
        )
        assert isinstance(result, ExternalAuthCompleteResponse)
        assert (
            result.redirect_uri
            == "https://your-authkit-domain.workos.com/oauth/authorize/complete?state=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdGF0ZSI6InJhbmRvbV9zdGF0ZV9zdHJpbmciLCJpYXQiOjE3NDI2MDQ4NTN9.abc123def456ghi789"
        )
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/authkit/oauth2/complete")

    async def test_complete_login_unauthorized(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationException):
            await async_workos.workos_connect.complete_login(
                external_auth_id="test_external_auth_id",
                user=UserObject.from_dict(load_fixture("user_object.json")),
            )

    async def test_complete_login_not_found(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                await workos.workos_connect.complete_login(
                    external_auth_id="test_external_auth_id",
                    user=UserObject.from_dict(load_fixture("user_object.json")),
                )
        finally:
            await workos.close()

    async def test_complete_login_rate_limited(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                await workos.workos_connect.complete_login(
                    external_auth_id="test_external_auth_id",
                    user=UserObject.from_dict(load_fixture("user_object.json")),
                )
        finally:
            await workos.close()

    async def test_complete_login_server_error(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                await workos.workos_connect.complete_login(
                    external_auth_id="test_external_auth_id",
                    user=UserObject.from_dict(load_fixture("user_object.json")),
                )
        finally:
            await workos.close()
