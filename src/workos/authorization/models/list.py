# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from typing import cast
from typing import Any, Dict, List, Literal
from workos._errors import WorkOSError

from .list_data import ListData


@dataclass(slots=True)
class ListModel:
    """List Model model."""

    object: Literal["list"]
    data: List["ListData"]
    """The list of records for the current page."""

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> "ListModel":
        """Deserialize from a dictionary."""
        try:
            return cls(
                object=data["object"],
                data=[
                    ListData.from_dict(cast(Dict[str, Any], item))
                    for item in cast(list[Any], data["data"])
                ],
            )
        except (KeyError, ValueError) as e:
            raise WorkOSError(
                f"Unexpected API response while parsing ListModel: {e!s}"
            ) from e

    def to_dict(self) -> Dict[str, Any]:
        """Serialize to a dictionary."""
        result: Dict[str, Any] = {}
        result["object"] = self.object
        result["data"] = [item.to_dict() for item in self.data]
        return result
