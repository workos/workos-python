# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, Optional

if TYPE_CHECKING:
    from ..._client import AsyncWorkOSClient, WorkOSClient

from ..._types import RequestOptions
from .models import AuthenticationChallengeVerifyResponse


class MultiFactorAuthChallenges:
    """Multi Factor Auth Challenges API resources."""

    def __init__(self, client: "WorkOSClient") -> None:
        self._client = client

    def verify(
        self,
        id: str,
        *,
        code: str,
        request_options: Optional[RequestOptions] = None,
    ) -> AuthenticationChallengeVerifyResponse:
        """Verify Challenge

        Verifies an Authentication Challenge.

                Args:
                    id: The unique ID of the Authentication Challenge.
                    code: The one-time code to verify.
                    request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

                Returns:
                    AuthenticationChallengeVerifyResponse

                Raises:
                    BadRequestException: If the request is malformed (400).
                    NotFoundException: If the resource is not found (404).
                    UnprocessableEntityException: If the request data is unprocessable (422).
                    AuthenticationException: If the API key is invalid (401).
                    RateLimitExceededException: If rate limited (429).
                    ServerException: If the server returns a 5xx error.
        """
        body: Dict[str, Any] = {
            "code": code,
        }
        return self._client.request(
            method="post",
            path=f"auth/challenges/{id}/verify",
            body=body,
            model=AuthenticationChallengeVerifyResponse,
            request_options=request_options,
        )


class AsyncMultiFactorAuthChallenges:
    """Multi Factor Auth Challenges API resources (async)."""

    def __init__(self, client: "AsyncWorkOSClient") -> None:
        self._client = client

    async def verify(
        self,
        id: str,
        *,
        code: str,
        request_options: Optional[RequestOptions] = None,
    ) -> AuthenticationChallengeVerifyResponse:
        """Verify Challenge

        Verifies an Authentication Challenge.

                Args:
                    id: The unique ID of the Authentication Challenge.
                    code: The one-time code to verify.
                    request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

                Returns:
                    AuthenticationChallengeVerifyResponse

                Raises:
                    BadRequestException: If the request is malformed (400).
                    NotFoundException: If the resource is not found (404).
                    UnprocessableEntityException: If the request data is unprocessable (422).
                    AuthenticationException: If the API key is invalid (401).
                    RateLimitExceededException: If rate limited (429).
                    ServerException: If the server returns a 5xx error.
        """
        body: Dict[str, Any] = {
            "code": code,
        }
        return await self._client.request(
            method="post",
            path=f"auth/challenges/{id}/verify",
            body=body,
            model=AuthenticationChallengeVerifyResponse,
            request_options=request_options,
        )
