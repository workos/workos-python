# This file is auto-generated by oagen. Do not edit.


import pytest

from tests.generated_helpers import load_fixture
from workos import AsyncWorkOSClient, WorkOSClient
from workos._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)
from workos.common.models import ConnectedAccount, DataIntegrationsListResponse


class TestOrganizationsDataProviders:
    def test_get_organization_connected_account(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("connected_account.json"),
        )
        result = workos.organizations_data_providers.get_organization_connected_account(
            "test_organization_id", "test_slug"
        )
        assert isinstance(result, ConnectedAccount)
        assert result.object == "connected_account"
        assert result.id == "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/organizations/test_organization_id/connected_accounts/test_slug"
        )

    def test_get_organization_connected_account_encodes_query_params(
        self, workos, httpx_mock
    ):
        httpx_mock.add_response(json=load_fixture("connected_account.json"))
        workos.organizations_data_providers.get_organization_connected_account(
            "test_organization_id",
            "test_slug",
            supports_multiple_connections=True,
            connected_account_id="value connected_account_id/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["supports_multiple_connections"] == "true"
        assert (
            request.url.params["connected_account_id"]
            == "value connected_account_id/test"
        )

    def test_create_organization_connected_account(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("connected_account.json"),
        )
        result = (
            workos.organizations_data_providers.create_organization_connected_account(
                "test_organization_id", "test_slug"
            )
        )
        assert isinstance(result, ConnectedAccount)
        assert result.object == "connected_account"
        assert result.id == "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/organizations/test_organization_id/connected_accounts/test_slug"
        )

    def test_update_organization_connected_account(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("connected_account.json"),
        )
        result = (
            workos.organizations_data_providers.update_organization_connected_account(
                "test_organization_id", "test_slug"
            )
        )
        assert isinstance(result, ConnectedAccount)
        assert result.object == "connected_account"
        assert result.id == "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/organizations/test_organization_id/connected_accounts/test_slug"
        )

    def test_update_organization_connected_account_encodes_query_params(
        self, workos, httpx_mock
    ):
        httpx_mock.add_response(json=load_fixture("connected_account.json"))
        workos.organizations_data_providers.update_organization_connected_account(
            "test_organization_id",
            "test_slug",
            supports_multiple_connections=True,
            connected_account_id="value connected_account_id/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["supports_multiple_connections"] == "true"
        assert (
            request.url.params["connected_account_id"]
            == "value connected_account_id/test"
        )

    def test_delete_organization_connected_account(self, workos, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = (
            workos.organizations_data_providers.delete_organization_connected_account(
                "test_organization_id", "test_slug"
            )
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/organizations/test_organization_id/connected_accounts/test_slug"
        )

    def test_delete_organization_connected_account_encodes_query_params(
        self, workos, httpx_mock
    ):
        httpx_mock.add_response(status_code=204)
        workos.organizations_data_providers.delete_organization_connected_account(
            "test_organization_id",
            "test_slug",
            supports_multiple_connections=True,
            connected_account_id="value connected_account_id/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["supports_multiple_connections"] == "true"
        assert (
            request.url.params["connected_account_id"]
            == "value connected_account_id/test"
        )

    def test_list_organization_data_providers(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("data_integrations_list_response.json"),
        )
        result = workos.organizations_data_providers.list_organization_data_providers(
            "test_organization_id"
        )
        assert isinstance(result, DataIntegrationsListResponse)
        assert result.object == "list"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/organizations/test_organization_id/data_providers"
        )

    def test_list_organization_data_providers_encodes_query_params(
        self, workos, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture("data_integrations_list_response.json")
        )
        workos.organizations_data_providers.list_organization_data_providers(
            "test_organization_id", supports_multiple_connections=True
        )
        request = httpx_mock.get_request()
        assert request.url.params["supports_multiple_connections"] == "true"

    def test_get_organization_connected_account_with_request_options(
        self, workos, httpx_mock
    ):
        httpx_mock.add_response(json=load_fixture("connected_account.json"))
        workos.organizations_data_providers.get_organization_connected_account(
            "test_organization_id",
            "test_slug",
            request_options={"extra_headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_get_organization_connected_account_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            workos.organizations_data_providers.get_organization_connected_account(
                "test_organization_id", "test_slug"
            )

    def test_get_organization_connected_account_not_found(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                workos.organizations_data_providers.get_organization_connected_account(
                    "test_organization_id", "test_slug"
                )
        finally:
            workos.close()

    def test_get_organization_connected_account_rate_limited(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                workos.organizations_data_providers.get_organization_connected_account(
                    "test_organization_id", "test_slug"
                )
        finally:
            workos.close()

    def test_get_organization_connected_account_server_error(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                workos.organizations_data_providers.get_organization_connected_account(
                    "test_organization_id", "test_slug"
                )
        finally:
            workos.close()

    def test_get_organization_connected_account_bad_request(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                workos.organizations_data_providers.get_organization_connected_account(
                    "test_organization_id", "test_slug"
                )
        finally:
            workos.close()

    def test_get_organization_connected_account_unprocessable(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                workos.organizations_data_providers.get_organization_connected_account(
                    "test_organization_id", "test_slug"
                )
        finally:
            workos.close()


class TestAsyncOrganizationsDataProviders:
    @pytest.mark.asyncio
    async def test_get_organization_connected_account(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("connected_account.json"))
        result = await async_workos.organizations_data_providers.get_organization_connected_account(
            "test_organization_id", "test_slug"
        )
        assert isinstance(result, ConnectedAccount)
        assert result.object == "connected_account"
        assert result.id == "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/organizations/test_organization_id/connected_accounts/test_slug"
        )

    @pytest.mark.asyncio
    async def test_get_organization_connected_account_encodes_query_params(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(json=load_fixture("connected_account.json"))
        await async_workos.organizations_data_providers.get_organization_connected_account(
            "test_organization_id",
            "test_slug",
            supports_multiple_connections=True,
            connected_account_id="value connected_account_id/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["supports_multiple_connections"] == "true"
        assert (
            request.url.params["connected_account_id"]
            == "value connected_account_id/test"
        )

    @pytest.mark.asyncio
    async def test_create_organization_connected_account(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(json=load_fixture("connected_account.json"))
        result = await async_workos.organizations_data_providers.create_organization_connected_account(
            "test_organization_id", "test_slug"
        )
        assert isinstance(result, ConnectedAccount)
        assert result.object == "connected_account"
        assert result.id == "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/organizations/test_organization_id/connected_accounts/test_slug"
        )

    @pytest.mark.asyncio
    async def test_update_organization_connected_account(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(json=load_fixture("connected_account.json"))
        result = await async_workos.organizations_data_providers.update_organization_connected_account(
            "test_organization_id", "test_slug"
        )
        assert isinstance(result, ConnectedAccount)
        assert result.object == "connected_account"
        assert result.id == "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "PUT"
        assert request.url.path.endswith(
            "/organizations/test_organization_id/connected_accounts/test_slug"
        )

    @pytest.mark.asyncio
    async def test_update_organization_connected_account_encodes_query_params(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(json=load_fixture("connected_account.json"))
        await async_workos.organizations_data_providers.update_organization_connected_account(
            "test_organization_id",
            "test_slug",
            supports_multiple_connections=True,
            connected_account_id="value connected_account_id/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["supports_multiple_connections"] == "true"
        assert (
            request.url.params["connected_account_id"]
            == "value connected_account_id/test"
        )

    @pytest.mark.asyncio
    async def test_delete_organization_connected_account(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(status_code=204)
        result = await async_workos.organizations_data_providers.delete_organization_connected_account(
            "test_organization_id", "test_slug"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/organizations/test_organization_id/connected_accounts/test_slug"
        )

    @pytest.mark.asyncio
    async def test_delete_organization_connected_account_encodes_query_params(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(status_code=204)
        await async_workos.organizations_data_providers.delete_organization_connected_account(
            "test_organization_id",
            "test_slug",
            supports_multiple_connections=True,
            connected_account_id="value connected_account_id/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["supports_multiple_connections"] == "true"
        assert (
            request.url.params["connected_account_id"]
            == "value connected_account_id/test"
        )

    @pytest.mark.asyncio
    async def test_list_organization_data_providers(self, async_workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("data_integrations_list_response.json")
        )
        result = await async_workos.organizations_data_providers.list_organization_data_providers(
            "test_organization_id"
        )
        assert isinstance(result, DataIntegrationsListResponse)
        assert result.object == "list"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/organizations/test_organization_id/data_providers"
        )

    @pytest.mark.asyncio
    async def test_list_organization_data_providers_encodes_query_params(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture("data_integrations_list_response.json")
        )
        await (
            async_workos.organizations_data_providers.list_organization_data_providers(
                "test_organization_id", supports_multiple_connections=True
            )
        )
        request = httpx_mock.get_request()
        assert request.url.params["supports_multiple_connections"] == "true"

    @pytest.mark.asyncio
    async def test_get_organization_connected_account_with_request_options(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(json=load_fixture("connected_account.json"))
        await async_workos.organizations_data_providers.get_organization_connected_account(
            "test_organization_id",
            "test_slug",
            request_options={"extra_headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_get_organization_connected_account_unauthorized(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_workos.organizations_data_providers.get_organization_connected_account(
                "test_organization_id", "test_slug"
            )

    @pytest.mark.asyncio
    async def test_get_organization_connected_account_not_found(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await workos.organizations_data_providers.get_organization_connected_account(
                    "test_organization_id", "test_slug"
                )
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_get_organization_connected_account_rate_limited(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await workos.organizations_data_providers.get_organization_connected_account(
                    "test_organization_id", "test_slug"
                )
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_get_organization_connected_account_server_error(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await workos.organizations_data_providers.get_organization_connected_account(
                    "test_organization_id", "test_slug"
                )
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_get_organization_connected_account_bad_request(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await workos.organizations_data_providers.get_organization_connected_account(
                    "test_organization_id", "test_slug"
                )
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_get_organization_connected_account_unprocessable(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await workos.organizations_data_providers.get_organization_connected_account(
                    "test_organization_id", "test_slug"
                )
        finally:
            await workos.close()
