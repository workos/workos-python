# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, Optional, Union

if TYPE_CHECKING:
    from .._client import AsyncWorkOSClient, WorkOSClient

from .._types import RequestOptions, enum_value
from .models import FlagTarget
from workos.common.models.pagination_order import PaginationOrder
from .._pagination import AsyncPage, SyncPage


class FlagTargets:
    """Flag Targets API resources."""

    def __init__(self, client: "WorkOSClient") -> None:
        self._client = client

    def list_flag_targets(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[Union[PaginationOrder, str]] = "desc",
        flag_slug: Optional[str] = None,
        target_type: Optional[str] = None,
        target_id: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> SyncPage[FlagTarget]:
        """List flag targets

        Get a list of the Flag Targets in the current environment, optionally filtered by Feature Flag slug, target type, or targeted entity.

        Args:
            limit: Upper limit on the number of objects to return, between `1` and `100`. Defaults to `10`.
            before: An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
            after: An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
            order: Order the results by the creation time. Defaults to `desc`.
            flag_slug: Filter Flag Targets by the slug of the Feature Flag they belong to.
            target_type: Filter Flag Targets by the type of the targeted entity, to find every Feature Flag targeting entities of that type.
            target_id: Filter Flag Targets by the ID of the targeted entity, to find every Feature Flag targeting it.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            SyncPage[FlagTarget]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": enum_value(order) if order is not None else None,
                "flag_slug": flag_slug,
                "target_type": target_type,
                "target_id": target_id,
            }.items()
            if v is not None
        }
        return self._client.request_page(
            method="get",
            path=("flag_targets",),
            model=FlagTarget,
            params=params,
            request_options=request_options,
        )

    def create_flag_target(
        self,
        *,
        flag_slug: str,
        target_type: str,
        target_id: str,
        request_options: Optional[RequestOptions] = None,
    ) -> FlagTarget:
        """Create a flag target

        Enable a Feature Flag for a target in the current environment. The `target_type` is given explicitly: a custom target type has to be registered with `POST /flag_target_types` first, and its `target_id` is an identifier from your own domain model, which carries no prefix to infer a type from.

        Args:
            flag_slug: The slug of the Feature Flag to target.
            target_type: The type of the target: `organization`, `user`, or the slug of a Flag Target Type registered for the project.
            target_id: The ID of the entity to target. For the built-in target types this is a WorkOS Organization or User ID; for a custom target type it is an identifier from your own domain model.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            FlagTarget

        Raises:
            BadRequestError: If the request is malformed (400).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        body: Dict[str, Any] = {
            "flag_slug": flag_slug,
            "target_type": target_type,
            "target_id": target_id,
        }
        return self._client.request(
            method="post",
            path=("flag_targets",),
            body=body,
            model=FlagTarget,
            request_options=request_options,
        )

    def get_flag_target(
        self,
        id: str,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> FlagTarget:
        """Get a flag target

        Get the details of an existing Flag Target.

        Args:
            id: Unique identifier of the Flag Target.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            FlagTarget

        Raises:
            NotFoundError: If the resource is not found (404).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        return self._client.request(
            method="get",
            path=("flag_targets", str(id)),
            model=FlagTarget,
            request_options=request_options,
        )

    def delete_flag_target(
        self,
        id: str,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a flag target

        Disable a Feature Flag for a target by deleting the Flag Target.

        Args:
            id: Unique identifier of the Flag Target.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            NotFoundError: If the resource is not found (404).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        self._client.request(
            method="delete",
            path=("flag_targets", str(id)),
            request_options=request_options,
        )


class AsyncFlagTargets:
    """Flag Targets API resources (async)."""

    def __init__(self, client: "AsyncWorkOSClient") -> None:
        self._client = client

    async def list_flag_targets(
        self,
        *,
        limit: Optional[int] = None,
        before: Optional[str] = None,
        after: Optional[str] = None,
        order: Optional[Union[PaginationOrder, str]] = "desc",
        flag_slug: Optional[str] = None,
        target_type: Optional[str] = None,
        target_id: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> AsyncPage[FlagTarget]:
        """List flag targets

        Get a list of the Flag Targets in the current environment, optionally filtered by Feature Flag slug, target type, or targeted entity.

        Args:
            limit: Upper limit on the number of objects to return, between `1` and `100`. Defaults to `10`.
            before: An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `before="obj_123"` to fetch a new batch of objects before `"obj_123"`.
            after: An object ID that defines your place in the list. When the ID is not present, you are at the end of the list. For example, if you make a list request and receive 100 objects, ending with `"obj_123"`, your subsequent call can include `after="obj_123"` to fetch a new batch of objects after `"obj_123"`.
            order: Order the results by the creation time. Defaults to `desc`.
            flag_slug: Filter Flag Targets by the slug of the Feature Flag they belong to.
            target_type: Filter Flag Targets by the type of the targeted entity, to find every Feature Flag targeting entities of that type.
            target_id: Filter Flag Targets by the ID of the targeted entity, to find every Feature Flag targeting it.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            AsyncPage[FlagTarget]

        Raises:
            BadRequestError: If the request is malformed (400).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        params = {
            k: v
            for k, v in {
                "limit": limit,
                "before": before,
                "after": after,
                "order": enum_value(order) if order is not None else None,
                "flag_slug": flag_slug,
                "target_type": target_type,
                "target_id": target_id,
            }.items()
            if v is not None
        }
        return await self._client.request_page(
            method="get",
            path=("flag_targets",),
            model=FlagTarget,
            params=params,
            request_options=request_options,
        )

    async def create_flag_target(
        self,
        *,
        flag_slug: str,
        target_type: str,
        target_id: str,
        request_options: Optional[RequestOptions] = None,
    ) -> FlagTarget:
        """Create a flag target

        Enable a Feature Flag for a target in the current environment. The `target_type` is given explicitly: a custom target type has to be registered with `POST /flag_target_types` first, and its `target_id` is an identifier from your own domain model, which carries no prefix to infer a type from.

        Args:
            flag_slug: The slug of the Feature Flag to target.
            target_type: The type of the target: `organization`, `user`, or the slug of a Flag Target Type registered for the project.
            target_id: The ID of the entity to target. For the built-in target types this is a WorkOS Organization or User ID; for a custom target type it is an identifier from your own domain model.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            FlagTarget

        Raises:
            BadRequestError: If the request is malformed (400).
            NotFoundError: If the resource is not found (404).
            UnprocessableEntityError: If the request data is unprocessable (422).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        body: Dict[str, Any] = {
            "flag_slug": flag_slug,
            "target_type": target_type,
            "target_id": target_id,
        }
        return await self._client.request(
            method="post",
            path=("flag_targets",),
            body=body,
            model=FlagTarget,
            request_options=request_options,
        )

    async def get_flag_target(
        self,
        id: str,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> FlagTarget:
        """Get a flag target

        Get the details of an existing Flag Target.

        Args:
            id: Unique identifier of the Flag Target.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            FlagTarget

        Raises:
            NotFoundError: If the resource is not found (404).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        return await self._client.request(
            method="get",
            path=("flag_targets", str(id)),
            model=FlagTarget,
            request_options=request_options,
        )

    async def delete_flag_target(
        self,
        id: str,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> None:
        """Delete a flag target

        Disable a Feature Flag for a target by deleting the Flag Target.

        Args:
            id: Unique identifier of the Flag Target.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Raises:
            NotFoundError: If the resource is not found (404).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        await self._client.request(
            method="delete",
            path=("flag_targets", str(id)),
            request_options=request_options,
        )
