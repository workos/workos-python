# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from typing import Any, cast

from workos._types import _raise_deserialize_error

from .agent_blueprints_update_request_invocable_by import (
    AgentBlueprintsUpdateRequestInvocableBy,
)
from .agent_blueprints_update_request_session_setting import (
    AgentBlueprintsUpdateRequestSessionSetting,
)


@dataclass(slots=True)
class AgentBlueprintsUpdateRequest:
    """Agent Blueprints Update Request model."""

    name: str | None = None
    """Human-readable name of the agent blueprint."""
    description: str | None = None
    """Human-readable description of the agent blueprint. Pass `null` to clear it."""
    permissions: list[str] | None = None
    """Permission slugs forming the ceiling on what sessions minted from this blueprint may do. Each slug must exist in the environment."""
    invocable_by: AgentBlueprintsUpdateRequestInvocableBy | None = None
    """Who may mint sessions from this blueprint. Omitted lists are left unchanged."""
    session_settings: AgentBlueprintsUpdateRequestSessionSetting | None = None
    """Token and session lifetimes for sessions minted from this blueprint. Omitted fields are left unchanged."""

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> AgentBlueprintsUpdateRequest:
        """Deserialize from a dictionary."""
        try:
            return cls(
                name=data.get("name"),
                description=data.get("description"),
                permissions=data.get("permissions"),
                invocable_by=AgentBlueprintsUpdateRequestInvocableBy.from_dict(
                    cast(dict[str, Any], _v_invocable_by)
                )
                if (_v_invocable_by := data.get("invocable_by")) is not None
                else None,
                session_settings=AgentBlueprintsUpdateRequestSessionSetting.from_dict(
                    cast(dict[str, Any], _v_session_settings)
                )
                if (_v_session_settings := data.get("session_settings")) is not None
                else None,
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error("AgentBlueprintsUpdateRequest", e)

    def to_dict(self) -> dict[str, Any]:
        """Serialize to a dictionary."""
        result: dict[str, Any] = {}
        if self.name is not None:
            result["name"] = self.name
        if self.description is not None:
            result["description"] = self.description
        else:
            result["description"] = None
        if self.permissions is not None:
            result["permissions"] = self.permissions
        if self.invocable_by is not None:
            result["invocable_by"] = self.invocable_by.to_dict()
        if self.session_settings is not None:
            result["session_settings"] = self.session_settings.to_dict()
        return result
