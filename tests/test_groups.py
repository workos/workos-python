# This file is auto-generated by oagen. Do not edit.

import json

import pytest
from workos import WorkOSClient, AsyncWorkOSClient
from tests.generated_helpers import load_fixture

from workos.common.models import (
    Group,
    UserOrganizationMembershipBaseListData,
    PaginationOrder,
)
from workos._pagination import AsyncPage, SyncPage
from workos._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)


class TestGroups:
    def test_list_organization_groups(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("list_group.json"),
        )
        page = workos.groups.list_organization_groups("test_organizationId")
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(page.data[0], Group)

    def test_list_organization_groups_empty_page(self, workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = workos.groups.list_organization_groups("test_organizationId")
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_organization_groups_encodes_query_params(self, workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        workos.groups.list_organization_groups(
            "test_organizationId",
            limit=10,
            before="cursor before",
            after="cursor/after",
            order=PaginationOrder("value_order"),
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["order"] == "value_order"

    def test_create_organization_group(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("group.json"),
        )
        result = workos.groups.create_organization_group(
            "test_organizationId", name="test_name"
        )
        assert isinstance(result, Group)
        assert result.object == "group"
        assert result.id == "group_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/organizations/test_organizationId/groups")
        body = json.loads(request.content)
        assert body["name"] == "test_name"

    def test_get_organization_group(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("group.json"),
        )
        result = workos.groups.get_organization_group(
            "test_organizationId", "test_groupId"
        )
        assert isinstance(result, Group)
        assert result.object == "group"
        assert result.id == "group_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/organizations/test_organizationId/groups/test_groupId"
        )

    def test_update_organization_group(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("group.json"),
        )
        result = workos.groups.update_organization_group(
            "test_organizationId", "test_groupId"
        )
        assert isinstance(result, Group)
        assert result.object == "group"
        assert result.id == "group_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith(
            "/organizations/test_organizationId/groups/test_groupId"
        )

    def test_delete_organization_group(self, workos, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = workos.groups.delete_organization_group(
            "test_organizationId", "test_groupId"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/organizations/test_organizationId/groups/test_groupId"
        )

    def test_list_group_organization_memberships(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("list_user_organization_membership_base_list_data.json"),
        )
        page = workos.groups.list_group_organization_memberships(
            "test_organizationId", "test_groupId"
        )
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(page.data[0], UserOrganizationMembershipBaseListData)

    def test_list_group_organization_memberships_empty_page(self, workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = workos.groups.list_group_organization_memberships(
            "test_organizationId", "test_groupId"
        )
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_group_organization_memberships_encodes_query_params(
        self, workos, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        workos.groups.list_group_organization_memberships(
            "test_organizationId",
            "test_groupId",
            limit=10,
            before="cursor before",
            after="cursor/after",
            order=PaginationOrder("value_order"),
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["order"] == "value_order"

    def test_create_group_organization_membership(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("group.json"),
        )
        result = workos.groups.create_group_organization_membership(
            "test_organizationId",
            "test_groupId",
            organization_membership_id="test_organization_membership_id",
        )
        assert isinstance(result, Group)
        assert result.object == "group"
        assert result.id == "group_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/organizations/test_organizationId/groups/test_groupId/organization-memberships"
        )
        body = json.loads(request.content)
        assert body["organization_membership_id"] == "test_organization_membership_id"

    def test_delete_group_organization_membership(self, workos, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = workos.groups.delete_group_organization_membership(
            "test_organizationId", "test_groupId", "test_omId"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/organizations/test_organizationId/groups/test_groupId/organization-memberships/test_omId"
        )

    def test_list_organization_groups_with_request_options(self, workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        workos.groups.list_organization_groups(
            "test_organizationId",
            request_options={"extra_headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_organization_groups_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            workos.groups.list_organization_groups("test_organizationId")

    def test_list_organization_groups_not_found(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                workos.groups.list_organization_groups("test_organizationId")
        finally:
            workos.close()

    def test_list_organization_groups_rate_limited(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                workos.groups.list_organization_groups("test_organizationId")
        finally:
            workos.close()

    def test_list_organization_groups_server_error(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                workos.groups.list_organization_groups("test_organizationId")
        finally:
            workos.close()

    def test_list_organization_groups_bad_request(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                workos.groups.list_organization_groups("test_organizationId")
        finally:
            workos.close()

    def test_list_organization_groups_unprocessable(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                workos.groups.list_organization_groups("test_organizationId")
        finally:
            workos.close()


class TestAsyncGroups:
    @pytest.mark.asyncio
    async def test_list_organization_groups(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("list_group.json"))
        page = await async_workos.groups.list_organization_groups("test_organizationId")
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(page.data[0], Group)

    @pytest.mark.asyncio
    async def test_list_organization_groups_empty_page(self, async_workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_workos.groups.list_organization_groups("test_organizationId")
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_list_organization_groups_encodes_query_params(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_workos.groups.list_organization_groups(
            "test_organizationId",
            limit=10,
            before="cursor before",
            after="cursor/after",
            order=PaginationOrder("value_order"),
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["order"] == "value_order"

    @pytest.mark.asyncio
    async def test_create_organization_group(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("group.json"))
        result = await async_workos.groups.create_organization_group(
            "test_organizationId", name="test_name"
        )
        assert isinstance(result, Group)
        assert result.object == "group"
        assert result.id == "group_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/organizations/test_organizationId/groups")

    @pytest.mark.asyncio
    async def test_get_organization_group(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("group.json"))
        result = await async_workos.groups.get_organization_group(
            "test_organizationId", "test_groupId"
        )
        assert isinstance(result, Group)
        assert result.object == "group"
        assert result.id == "group_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/organizations/test_organizationId/groups/test_groupId"
        )

    @pytest.mark.asyncio
    async def test_update_organization_group(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("group.json"))
        result = await async_workos.groups.update_organization_group(
            "test_organizationId", "test_groupId"
        )
        assert isinstance(result, Group)
        assert result.object == "group"
        assert result.id == "group_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "PATCH"
        assert request.url.path.endswith(
            "/organizations/test_organizationId/groups/test_groupId"
        )

    @pytest.mark.asyncio
    async def test_delete_organization_group(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = await async_workos.groups.delete_organization_group(
            "test_organizationId", "test_groupId"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/organizations/test_organizationId/groups/test_groupId"
        )

    @pytest.mark.asyncio
    async def test_list_group_organization_memberships(self, async_workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("list_user_organization_membership_base_list_data.json")
        )
        page = await async_workos.groups.list_group_organization_memberships(
            "test_organizationId", "test_groupId"
        )
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(page.data[0], UserOrganizationMembershipBaseListData)

    @pytest.mark.asyncio
    async def test_list_group_organization_memberships_empty_page(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_workos.groups.list_group_organization_memberships(
            "test_organizationId", "test_groupId"
        )
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_list_group_organization_memberships_encodes_query_params(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_workos.groups.list_group_organization_memberships(
            "test_organizationId",
            "test_groupId",
            limit=10,
            before="cursor before",
            after="cursor/after",
            order=PaginationOrder("value_order"),
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["order"] == "value_order"

    @pytest.mark.asyncio
    async def test_create_group_organization_membership(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("group.json"))
        result = await async_workos.groups.create_group_organization_membership(
            "test_organizationId",
            "test_groupId",
            organization_membership_id="test_organization_membership_id",
        )
        assert isinstance(result, Group)
        assert result.object == "group"
        assert result.id == "group_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith(
            "/organizations/test_organizationId/groups/test_groupId/organization-memberships"
        )

    @pytest.mark.asyncio
    async def test_delete_group_organization_membership(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = await async_workos.groups.delete_group_organization_membership(
            "test_organizationId", "test_groupId", "test_omId"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/organizations/test_organizationId/groups/test_groupId/organization-memberships/test_omId"
        )

    @pytest.mark.asyncio
    async def test_list_organization_groups_with_request_options(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_workos.groups.list_organization_groups(
            "test_organizationId",
            request_options={"extra_headers": {"X-Custom": "value"}},
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_organization_groups_unauthorized(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_workos.groups.list_organization_groups("test_organizationId")

    @pytest.mark.asyncio
    async def test_list_organization_groups_not_found(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await workos.groups.list_organization_groups("test_organizationId")
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_list_organization_groups_rate_limited(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await workos.groups.list_organization_groups("test_organizationId")
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_list_organization_groups_server_error(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await workos.groups.list_organization_groups("test_organizationId")
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_list_organization_groups_bad_request(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await workos.groups.list_organization_groups("test_organizationId")
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_list_organization_groups_unprocessable(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await workos.groups.list_organization_groups("test_organizationId")
        finally:
            await workos.close()
