# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from typing import Any, Literal

from workos._types import _raise_deserialize_error


@dataclass(slots=True)
class AgentInstanceSessionRevokedData:
    """The event payload."""

    object: Literal["agent_instance_session"]
    """Distinguishes the agent instance session object."""
    id: str
    """Unique identifier of the agent instance session."""
    agent_instance_id: str
    """The agent instance the session belongs to."""
    organization_id: str
    """The organization the owning agent instance belongs to."""
    expires_at: str
    """Timestamp when the session expires."""
    revoked_at: str | None
    """Timestamp when the session was revoked; `null` if it has not been revoked."""
    created_at: str
    """The timestamp when the session was created."""
    updated_at: str
    """The timestamp when the session was last updated."""

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> AgentInstanceSessionRevokedData:
        """Deserialize from a dictionary."""
        try:
            return cls(
                object=data.get("object", "agent_instance_session"),
                id=data["id"],
                agent_instance_id=data["agent_instance_id"],
                organization_id=data["organization_id"],
                expires_at=data["expires_at"],
                revoked_at=data["revoked_at"],
                created_at=data["created_at"],
                updated_at=data["updated_at"],
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error("AgentInstanceSessionRevokedData", e)

    def to_dict(self) -> dict[str, Any]:
        """Serialize to a dictionary."""
        result: dict[str, Any] = {}
        result["object"] = self.object
        result["id"] = self.id
        result["agent_instance_id"] = self.agent_instance_id
        result["organization_id"] = self.organization_id
        result["expires_at"] = self.expires_at
        if self.revoked_at is not None:
            result["revoked_at"] = self.revoked_at
        else:
            result["revoked_at"] = None
        result["created_at"] = self.created_at
        result["updated_at"] = self.updated_at
        return result
