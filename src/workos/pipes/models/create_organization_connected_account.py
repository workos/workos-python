# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from datetime import datetime
from enum import Enum
from typing import Any, Literal

from workos._types import _format_datetime, _parse_datetime, _raise_deserialize_error
from workos.common.models.create_organization_connected_account_state import (
    CreateOrganizationConnectedAccountState,
)


@dataclass(slots=True)
class CreateOrganizationConnectedAccount:
    """Create Organization Connected Account model."""

    user_id: str
    """The [User](https://workos.com/docs/reference/authkit/user) identifier of the organization member on whose behalf the connected account is being imported or updated. The user must be an active member of the organization."""
    access_token: str | None = None
    """The OAuth access token for the connected account."""
    refresh_token: str | None = None
    """The OAuth refresh token for the connected account."""
    expires_at: datetime | None = None
    """The ISO-8601 timestamp when the access token expires. Required when `access_token` is provided for tokens that expire."""
    scopes: list[str] | None = None
    """The OAuth scopes granted for this connection."""
    state: CreateOrganizationConnectedAccountState | None = None
    """Explicitly set the state of the connected account. When omitted, the state is derived from the token combination provided."""
    connection_intent: Literal["add"] | None = None
    """Set to `add` to create another connected account. Omit this field for permanent compatibility behavior. Creating an additional connection is not yet available: until it is, `add` succeeds only when the owner has no connection for this integration, which creates the compatibility connection, and otherwise returns 404 `multiple_connections_unavailable`."""

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> CreateOrganizationConnectedAccount:
        """Deserialize from a dictionary."""
        try:
            return cls(
                user_id=data["user_id"],
                access_token=data.get("access_token"),
                refresh_token=data.get("refresh_token"),
                expires_at=_parse_datetime(_v_expires_at)
                if (_v_expires_at := data.get("expires_at")) is not None
                else None,
                scopes=data.get("scopes"),
                state=CreateOrganizationConnectedAccountState(_v_state)
                if (_v_state := data.get("state")) is not None
                else None,
                connection_intent=data.get("connection_intent"),
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error("CreateOrganizationConnectedAccount", e)

    def to_dict(self) -> dict[str, Any]:
        """Serialize to a dictionary."""
        result: dict[str, Any] = {}
        result["user_id"] = self.user_id
        if self.access_token is not None:
            result["access_token"] = self.access_token
        if self.refresh_token is not None:
            result["refresh_token"] = self.refresh_token
        if self.expires_at is not None:
            result["expires_at"] = _format_datetime(self.expires_at)
        if self.scopes is not None:
            result["scopes"] = self.scopes
        if self.state is not None:
            result["state"] = (
                self.state.value if isinstance(self.state, Enum) else self.state
            )
        if self.connection_intent is not None:
            result["connection_intent"] = self.connection_intent
        return result
