# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from datetime import datetime
from enum import Enum
from typing import Any, Literal

from workos._types import _format_datetime, _parse_datetime, _raise_deserialize_error
from workos.common.models.agent_instance_session_status import (
    AgentInstanceSessionStatus,
)


@dataclass(slots=True)
class AgentInstanceSession:
    """Agent Instance Session model."""

    object: Literal["agent_instance_session"]
    """Distinguishes the agent instance session object."""
    id: str
    """Unique identifier of the agent instance session."""
    agent_instance_id: str
    """The agent instance the session belongs to."""
    status: AgentInstanceSessionStatus
    """Derived from `revoked_at` and `expires_at` at read time; a revoked session stays `revoked` even after it expires."""
    expires_at: datetime
    """Timestamp when the session expires."""
    revoked_at: datetime | None
    """Timestamp when the session was revoked; `null` if it has not been revoked."""
    created_at: datetime
    """Timestamp when the session was created."""
    updated_at: datetime
    """Timestamp when the session was last updated."""

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> AgentInstanceSession:
        """Deserialize from a dictionary."""
        try:
            return cls(
                object=data.get("object", "agent_instance_session"),
                id=data["id"],
                agent_instance_id=data["agent_instance_id"],
                status=AgentInstanceSessionStatus(data["status"]),
                expires_at=_parse_datetime(data["expires_at"]),
                revoked_at=_parse_datetime(_v_revoked_at)
                if (_v_revoked_at := data["revoked_at"]) is not None
                else None,
                created_at=_parse_datetime(data["created_at"]),
                updated_at=_parse_datetime(data["updated_at"]),
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error("AgentInstanceSession", e)

    def to_dict(self) -> dict[str, Any]:
        """Serialize to a dictionary."""
        result: dict[str, Any] = {}
        result["object"] = self.object
        result["id"] = self.id
        result["agent_instance_id"] = self.agent_instance_id
        result["status"] = (
            self.status.value if isinstance(self.status, Enum) else self.status
        )
        result["expires_at"] = _format_datetime(self.expires_at)
        if self.revoked_at is not None:
            result["revoked_at"] = _format_datetime(self.revoked_at)
        else:
            result["revoked_at"] = None
        result["created_at"] = _format_datetime(self.created_at)
        result["updated_at"] = _format_datetime(self.updated_at)
        return result
