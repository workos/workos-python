# This file is auto-generated by oagen. Do not edit.

import json

import pytest
from workos import WorkOS, AsyncWorkOS
from tests.generated_helpers import load_fixture

from workos.user_management.cors_origins.models import CORSOriginResponse
from workos._errors import (
    AuthenticationException,
    NotFoundException,
    RateLimitExceededException,
    ServerException,
)


class TestUserManagementCorsOrigins:
    def test_create_cors_origin(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("cors_origin_response.json"),
        )
        result = workos.user_management.cors_origins.create_cors_origin(
            origin="test_origin"
        )
        assert isinstance(result, CORSOriginResponse)
        assert result.object == "cors_origin"
        assert result.id == "cors_origin_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/cors_origins")
        body = json.loads(request.content)
        assert body["origin"] == "test_origin"

    def test_create_cors_origin_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationException):
            workos.user_management.cors_origins.create_cors_origin(origin="test_origin")

    def test_create_cors_origin_not_found(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                workos.user_management.cors_origins.create_cors_origin(
                    origin="test_origin"
                )
        finally:
            workos.close()

    def test_create_cors_origin_rate_limited(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                workos.user_management.cors_origins.create_cors_origin(
                    origin="test_origin"
                )
        finally:
            workos.close()

    def test_create_cors_origin_server_error(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                workos.user_management.cors_origins.create_cors_origin(
                    origin="test_origin"
                )
        finally:
            workos.close()


@pytest.mark.asyncio
class TestAsyncUserManagementCorsOrigins:
    async def test_create_cors_origin(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("cors_origin_response.json"))
        result = await async_workos.user_management.cors_origins.create_cors_origin(
            origin="test_origin"
        )
        assert isinstance(result, CORSOriginResponse)
        assert result.object == "cors_origin"
        assert result.id == "cors_origin_01HXYZ123456789ABCDEFGHIJ"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/cors_origins")

    async def test_create_cors_origin_unauthorized(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationException):
            await async_workos.user_management.cors_origins.create_cors_origin(
                origin="test_origin"
            )

    async def test_create_cors_origin_not_found(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                await workos.user_management.cors_origins.create_cors_origin(
                    origin="test_origin"
                )
        finally:
            await workos.close()

    async def test_create_cors_origin_rate_limited(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                await workos.user_management.cors_origins.create_cors_origin(
                    origin="test_origin"
                )
        finally:
            await workos.close()

    async def test_create_cors_origin_server_error(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                await workos.user_management.cors_origins.create_cors_origin(
                    origin="test_origin"
                )
        finally:
            await workos.close()
