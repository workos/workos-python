# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from typing import Any, Dict, Literal
from workos._errors import WorkOSError


@dataclass(slots=True)
class ConnectionDomain:
    """Connection Domain model."""

    id: str
    """Unique identifier for the Connection Domain."""
    object: Literal["connection_domain"]
    """Distinguishes the Connection Domain object."""
    domain: str
    """The domain value."""

    @classmethod
    def from_dict(cls, data: Dict[str, Any]) -> "ConnectionDomain":
        """Deserialize from a dictionary."""
        try:
            return cls(
                id=data["id"],
                object=data["object"],
                domain=data["domain"],
            )
        except (KeyError, ValueError) as e:
            raise WorkOSError(
                f"Unexpected API response while parsing ConnectionDomain: {e!s}"
            ) from e

    def to_dict(self) -> Dict[str, Any]:
        """Serialize to a dictionary."""
        result: Dict[str, Any] = {}
        result["id"] = self.id
        result["object"] = self.object
        result["domain"] = self.domain
        return result
