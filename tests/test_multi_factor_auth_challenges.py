# This file is auto-generated by oagen. Do not edit.

import json

import pytest
from workos import WorkOS, AsyncWorkOS
from tests.generated_helpers import load_fixture

from workos.multi_factor_auth.challenges.models import (
    AuthenticationChallengeVerifyResponse,
)
from workos._errors import (
    AuthenticationException,
    NotFoundException,
    RateLimitExceededException,
    ServerException,
)


class TestMultiFactorAuthChallenges:
    def test_verify(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("authentication_challenge_verify_response.json"),
        )
        result = workos.multi_factor_auth.challenges.verify("test_id", code="test_code")
        assert isinstance(result, AuthenticationChallengeVerifyResponse)
        assert result.valid is True
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/auth/challenges/test_id/verify")
        body = json.loads(request.content)
        assert body["code"] == "test_code"

    def test_verify_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationException):
            workos.multi_factor_auth.challenges.verify("test_id", code="test_code")

    def test_verify_not_found(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                workos.multi_factor_auth.challenges.verify("test_id", code="test_code")
        finally:
            workos.close()

    def test_verify_rate_limited(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                workos.multi_factor_auth.challenges.verify("test_id", code="test_code")
        finally:
            workos.close()

    def test_verify_server_error(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                workos.multi_factor_auth.challenges.verify("test_id", code="test_code")
        finally:
            workos.close()


@pytest.mark.asyncio
class TestAsyncMultiFactorAuthChallenges:
    async def test_verify(self, async_workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("authentication_challenge_verify_response.json")
        )
        result = await async_workos.multi_factor_auth.challenges.verify(
            "test_id", code="test_code"
        )
        assert isinstance(result, AuthenticationChallengeVerifyResponse)
        assert result.valid is True
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/auth/challenges/test_id/verify")

    async def test_verify_unauthorized(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationException):
            await async_workos.multi_factor_auth.challenges.verify(
                "test_id", code="test_code"
            )

    async def test_verify_not_found(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                await workos.multi_factor_auth.challenges.verify(
                    "test_id", code="test_code"
                )
        finally:
            await workos.close()

    async def test_verify_rate_limited(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                await workos.multi_factor_auth.challenges.verify(
                    "test_id", code="test_code"
                )
        finally:
            await workos.close()

    async def test_verify_server_error(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                await workos.multi_factor_auth.challenges.verify(
                    "test_id", code="test_code"
                )
        finally:
            await workos.close()
