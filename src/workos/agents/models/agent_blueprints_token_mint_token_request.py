# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from enum import Enum
from typing import Any

from workos._types import _raise_deserialize_error
from workos.common.models.agent_blueprints_token_mint_token_request_type import (
    AgentBlueprintsTokenMintTokenRequestType,
)


@dataclass(slots=True)
class AgentBlueprintsTokenMintTokenRequest:
    """Agent Blueprints Token Mint Token Request model."""

    type: AgentBlueprintsTokenMintTokenRequestType
    """How the session is minted: `user_delegated`, `autonomous`, `agent_delegated`, or `refresh`."""
    user_access_token: str | None = None
    """The access token of the user delegating to the agent. The token identifies the user and organization; effective permissions are resolved server-side."""
    intent: str | None = None
    """Optional caller-supplied context, echoed as an object with a `text` field in the `intent` claim of the minted access token."""
    organization_id: str | None = None
    """The organization the agent acts within when operating as itself."""
    agent_access_token: str | None = None
    """The agent's own access token to exchange for a new session on the same instance. The token must have been minted from this blueprint; permissions are re-derived from current authority."""
    refresh_token: str | None = None
    """The refresh token issued with a previous agent access token. Refresh tokens are single-use: each refresh rotates it."""

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> AgentBlueprintsTokenMintTokenRequest:
        """Deserialize from a dictionary."""
        try:
            return cls(
                type=AgentBlueprintsTokenMintTokenRequestType(data["type"]),
                user_access_token=data.get("user_access_token"),
                intent=data.get("intent"),
                organization_id=data.get("organization_id"),
                agent_access_token=data.get("agent_access_token"),
                refresh_token=data.get("refresh_token"),
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error("AgentBlueprintsTokenMintTokenRequest", e)

    def to_dict(self) -> dict[str, Any]:
        """Serialize to a dictionary."""
        result: dict[str, Any] = {}
        result["type"] = self.type.value if isinstance(self.type, Enum) else self.type
        if self.user_access_token is not None:
            result["user_access_token"] = self.user_access_token
        if self.intent is not None:
            result["intent"] = self.intent
        if self.organization_id is not None:
            result["organization_id"] = self.organization_id
        if self.agent_access_token is not None:
            result["agent_access_token"] = self.agent_access_token
        if self.refresh_token is not None:
            result["refresh_token"] = self.refresh_token
        return result
