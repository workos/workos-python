# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from datetime import datetime
from typing import Any, Literal

from workos._types import _format_datetime, _parse_datetime, _raise_deserialize_error


@dataclass(slots=True)
class SAMLSpEncryptionCertificate:
    """SAML Sp Encryption Certificate model."""

    object: Literal["saml_sp_encryption_certificate"]
    """Distinguishes the SAML Service Provider encryption certificate object."""
    id: str
    """Unique identifier for the Service Provider encryption key pair. WorkOS holds the corresponding private key, which is never exposed."""
    value: str
    """The PEM-encoded public X.509 certificate."""
    not_before: datetime | None
    """When the certificate becomes valid."""
    not_after: datetime | None
    """When the certificate expires."""
    created_at: datetime
    """An ISO 8601 timestamp."""

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> SAMLSpEncryptionCertificate:
        """Deserialize from a dictionary."""
        try:
            return cls(
                object=data.get("object", "saml_sp_encryption_certificate"),
                id=data["id"],
                value=data["value"],
                not_before=_parse_datetime(_v_not_before)
                if (_v_not_before := data["not_before"]) is not None
                else None,
                not_after=_parse_datetime(_v_not_after)
                if (_v_not_after := data["not_after"]) is not None
                else None,
                created_at=_parse_datetime(data["created_at"]),
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error("SAMLSpEncryptionCertificate", e)

    def to_dict(self) -> dict[str, Any]:
        """Serialize to a dictionary."""
        result: dict[str, Any] = {}
        result["object"] = self.object
        result["id"] = self.id
        result["value"] = self.value
        if self.not_before is not None:
            result["not_before"] = _format_datetime(self.not_before)
        else:
            result["not_before"] = None
        if self.not_after is not None:
            result["not_after"] = _format_datetime(self.not_after)
        else:
            result["not_after"] = None
        result["created_at"] = _format_datetime(self.created_at)
        return result
