# This file is auto-generated by oagen. Do not edit.

import json

import pytest
from workos import WorkOSClient, AsyncWorkOSClient
from tests.generated_helpers import load_fixture

from workos.common.models import PaginationOrder
from workos.flag_targets.models import FlagTarget
from workos._pagination import AsyncPage, SyncPage
from workos._errors import (
    AuthenticationError,
    BadRequestError,
    NotFoundError,
    RateLimitExceededError,
    ServerError,
    UnprocessableEntityError,
)


class TestFlagTargets:
    def test_list_flag_targets(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("list_flag_target.json"),
        )
        page = workos.flag_targets.list_flag_targets()
        assert isinstance(page, SyncPage)
        assert len(page.data) == 1
        assert isinstance(page.data[0], FlagTarget)

    def test_list_flag_targets_empty_page(self, workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = workos.flag_targets.list_flag_targets()
        assert isinstance(page, SyncPage)
        assert page.data == []

    def test_list_flag_targets_encodes_query_params(self, workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        workos.flag_targets.list_flag_targets(
            limit=10,
            before="cursor before",
            after="cursor/after",
            order=PaginationOrder("value_order"),
            flag_slug="value flag_slug/test",
            target_type="value target_type/test",
            target_id="value target_id/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["order"] == "value_order"
        assert request.url.params["flag_slug"] == "value flag_slug/test"
        assert request.url.params["target_type"] == "value target_type/test"
        assert request.url.params["target_id"] == "value target_id/test"

    def test_create_flag_target(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("flag_target.json"),
        )
        result = workos.flag_targets.create_flag_target(
            flag_slug="test_flag_slug",
            target_type="test_target_type",
            target_id="test_target_id",
        )
        assert isinstance(result, FlagTarget)
        assert result.object == "flag_target"
        assert result.id == "flag_target_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/flag_targets")
        body = json.loads(request.content)
        assert body["flag_slug"] == "test_flag_slug"
        assert body["target_type"] == "test_target_type"
        assert body["target_id"] == "test_target_id"

    def test_get_flag_target(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("flag_target.json"),
        )
        result = workos.flag_targets.get_flag_target("test_id")
        assert isinstance(result, FlagTarget)
        assert result.object == "flag_target"
        assert result.id == "flag_target_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/flag_targets/test_id")

    def test_delete_flag_target(self, workos, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = workos.flag_targets.delete_flag_target("test_id")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/flag_targets/test_id")

    def test_list_flag_targets_with_request_options(self, workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        workos.flag_targets.list_flag_targets(
            request_options={"extra_headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    def test_list_flag_targets_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationError):
            workos.flag_targets.list_flag_targets()

    def test_list_flag_targets_not_found(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                workos.flag_targets.list_flag_targets()
        finally:
            workos.close()

    def test_list_flag_targets_rate_limited(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                workos.flag_targets.list_flag_targets()
        finally:
            workos.close()

    def test_list_flag_targets_server_error(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                workos.flag_targets.list_flag_targets()
        finally:
            workos.close()

    def test_list_flag_targets_bad_request(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                workos.flag_targets.list_flag_targets()
        finally:
            workos.close()

    def test_list_flag_targets_unprocessable(self, httpx_mock):
        workos = WorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                workos.flag_targets.list_flag_targets()
        finally:
            workos.close()


class TestAsyncFlagTargets:
    @pytest.mark.asyncio
    async def test_list_flag_targets(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("list_flag_target.json"))
        page = await async_workos.flag_targets.list_flag_targets()
        assert isinstance(page, AsyncPage)
        assert len(page.data) == 1
        assert isinstance(page.data[0], FlagTarget)

    @pytest.mark.asyncio
    async def test_list_flag_targets_empty_page(self, async_workos, httpx_mock):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        page = await async_workos.flag_targets.list_flag_targets()
        assert isinstance(page, AsyncPage)
        assert page.data == []

    @pytest.mark.asyncio
    async def test_list_flag_targets_encodes_query_params(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_workos.flag_targets.list_flag_targets(
            limit=10,
            before="cursor before",
            after="cursor/after",
            order=PaginationOrder("value_order"),
            flag_slug="value flag_slug/test",
            target_type="value target_type/test",
            target_id="value target_id/test",
        )
        request = httpx_mock.get_request()
        assert request.url.params["limit"] == "10"
        assert request.url.params["before"] == "cursor before"
        assert request.url.params["after"] == "cursor/after"
        assert request.url.params["order"] == "value_order"
        assert request.url.params["flag_slug"] == "value flag_slug/test"
        assert request.url.params["target_type"] == "value target_type/test"
        assert request.url.params["target_id"] == "value target_id/test"

    @pytest.mark.asyncio
    async def test_create_flag_target(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("flag_target.json"))
        result = await async_workos.flag_targets.create_flag_target(
            flag_slug="test_flag_slug",
            target_type="test_target_type",
            target_id="test_target_id",
        )
        assert isinstance(result, FlagTarget)
        assert result.object == "flag_target"
        assert result.id == "flag_target_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/flag_targets")

    @pytest.mark.asyncio
    async def test_get_flag_target(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("flag_target.json"))
        result = await async_workos.flag_targets.get_flag_target("test_id")
        assert isinstance(result, FlagTarget)
        assert result.object == "flag_target"
        assert result.id == "flag_target_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/flag_targets/test_id")

    @pytest.mark.asyncio
    async def test_delete_flag_target(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = await async_workos.flag_targets.delete_flag_target("test_id")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/flag_targets/test_id")

    @pytest.mark.asyncio
    async def test_list_flag_targets_with_request_options(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(json={"data": [], "list_metadata": {}})
        await async_workos.flag_targets.list_flag_targets(
            request_options={"extra_headers": {"X-Custom": "value"}}
        )
        request = httpx_mock.get_request()
        assert request.headers["X-Custom"] == "value"

    @pytest.mark.asyncio
    async def test_list_flag_targets_unauthorized(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationError):
            await async_workos.flag_targets.list_flag_targets()

    @pytest.mark.asyncio
    async def test_list_flag_targets_not_found(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundError):
                await workos.flag_targets.list_flag_targets()
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_list_flag_targets_rate_limited(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededError):
                await workos.flag_targets.list_flag_targets()
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_list_flag_targets_server_error(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerError):
                await workos.flag_targets.list_flag_targets()
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_list_flag_targets_bad_request(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=400, json={"message": "Bad request"})
            with pytest.raises(BadRequestError):
                await workos.flag_targets.list_flag_targets()
        finally:
            await workos.close()

    @pytest.mark.asyncio
    async def test_list_flag_targets_unprocessable(self, httpx_mock):
        workos = AsyncWorkOSClient(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=422, json={"message": "Unprocessable"})
            with pytest.raises(UnprocessableEntityError):
                await workos.flag_targets.list_flag_targets()
        finally:
            await workos.close()
