# This file is auto-generated by oagen. Do not edit.


import pytest
from workos import WorkOS, AsyncWorkOS
from tests.generated_helpers import load_fixture

from workos.user_management.data_providers.models import (
    ConnectedAccount,
    DataIntegrationsListResponse,
)
from workos._errors import (
    AuthenticationException,
    NotFoundException,
    RateLimitExceededException,
    ServerException,
)


class TestUserManagementDataProviders:
    def test_get_user_data_installation(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("connected_account.json"),
        )
        result = workos.user_management.data_providers.get_user_data_installation(
            "test_user_id", "test_slug"
        )
        assert isinstance(result, ConnectedAccount)
        assert result.object == "connected_account"
        assert result.id == "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/user_management/users/test_user_id/connected_accounts/test_slug"
        )

    def test_get_user_data_installation_encodes_query_params(self, workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("connected_account.json"))
        workos.user_management.data_providers.get_user_data_installation(
            "test_user_id", "test_slug", organization_id="value organization_id/test"
        )
        request = httpx_mock.get_request()
        assert request.url.params["organization_id"] == "value organization_id/test"

    def test_delete_user_data_installation(self, workos, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = workos.user_management.data_providers.delete_user_data_installation(
            "test_user_id", "test_slug"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/user_management/users/test_user_id/connected_accounts/test_slug"
        )

    def test_delete_user_data_installation_encodes_query_params(
        self, workos, httpx_mock
    ):
        httpx_mock.add_response(status_code=204)
        workos.user_management.data_providers.delete_user_data_installation(
            "test_user_id", "test_slug", organization_id="value organization_id/test"
        )
        request = httpx_mock.get_request()
        assert request.url.params["organization_id"] == "value organization_id/test"

    def test_get_user_data_integration(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("data_integrations_list_response.json"),
        )
        result = workos.user_management.data_providers.get_user_data_integration(
            "test_user_id"
        )
        assert isinstance(result, DataIntegrationsListResponse)
        assert result.object == "list"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/user_management/users/test_user_id/data_providers"
        )

    def test_get_user_data_integration_encodes_query_params(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("data_integrations_list_response.json")
        )
        workos.user_management.data_providers.get_user_data_integration(
            "test_user_id", organization_id="value organization_id/test"
        )
        request = httpx_mock.get_request()
        assert request.url.params["organization_id"] == "value organization_id/test"

    def test_get_user_data_installation_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationException):
            workos.user_management.data_providers.get_user_data_installation(
                "test_user_id", "test_slug"
            )

    def test_get_user_data_installation_not_found(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                workos.user_management.data_providers.get_user_data_installation(
                    "test_user_id", "test_slug"
                )
        finally:
            workos.close()

    def test_get_user_data_installation_rate_limited(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                workos.user_management.data_providers.get_user_data_installation(
                    "test_user_id", "test_slug"
                )
        finally:
            workos.close()

    def test_get_user_data_installation_server_error(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                workos.user_management.data_providers.get_user_data_installation(
                    "test_user_id", "test_slug"
                )
        finally:
            workos.close()


@pytest.mark.asyncio
class TestAsyncUserManagementDataProviders:
    async def test_get_user_data_installation(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("connected_account.json"))
        result = await async_workos.user_management.data_providers.get_user_data_installation(
            "test_user_id", "test_slug"
        )
        assert isinstance(result, ConnectedAccount)
        assert result.object == "connected_account"
        assert result.id == "data_installation_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/user_management/users/test_user_id/connected_accounts/test_slug"
        )

    async def test_get_user_data_installation_encodes_query_params(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(json=load_fixture("connected_account.json"))
        await async_workos.user_management.data_providers.get_user_data_installation(
            "test_user_id", "test_slug", organization_id="value organization_id/test"
        )
        request = httpx_mock.get_request()
        assert request.url.params["organization_id"] == "value organization_id/test"

    async def test_delete_user_data_installation(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = await async_workos.user_management.data_providers.delete_user_data_installation(
            "test_user_id", "test_slug"
        )
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith(
            "/user_management/users/test_user_id/connected_accounts/test_slug"
        )

    async def test_delete_user_data_installation_encodes_query_params(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(status_code=204)
        await async_workos.user_management.data_providers.delete_user_data_installation(
            "test_user_id", "test_slug", organization_id="value organization_id/test"
        )
        request = httpx_mock.get_request()
        assert request.url.params["organization_id"] == "value organization_id/test"

    async def test_get_user_data_integration(self, async_workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("data_integrations_list_response.json")
        )
        result = (
            await async_workos.user_management.data_providers.get_user_data_integration(
                "test_user_id"
            )
        )
        assert isinstance(result, DataIntegrationsListResponse)
        assert result.object == "list"
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith(
            "/user_management/users/test_user_id/data_providers"
        )

    async def test_get_user_data_integration_encodes_query_params(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(
            json=load_fixture("data_integrations_list_response.json")
        )
        await async_workos.user_management.data_providers.get_user_data_integration(
            "test_user_id", organization_id="value organization_id/test"
        )
        request = httpx_mock.get_request()
        assert request.url.params["organization_id"] == "value organization_id/test"

    async def test_get_user_data_installation_unauthorized(
        self, async_workos, httpx_mock
    ):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationException):
            await (
                async_workos.user_management.data_providers.get_user_data_installation(
                    "test_user_id", "test_slug"
                )
            )

    async def test_get_user_data_installation_not_found(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                await workos.user_management.data_providers.get_user_data_installation(
                    "test_user_id", "test_slug"
                )
        finally:
            await workos.close()

    async def test_get_user_data_installation_rate_limited(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                await workos.user_management.data_providers.get_user_data_installation(
                    "test_user_id", "test_slug"
                )
        finally:
            await workos.close()

    async def test_get_user_data_installation_server_error(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                await workos.user_management.data_providers.get_user_data_installation(
                    "test_user_id", "test_slug"
                )
        finally:
            await workos.close()
