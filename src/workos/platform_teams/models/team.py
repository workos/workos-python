# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from dataclasses import dataclass
from datetime import datetime
from enum import Enum
from typing import Any, Literal

from workos._types import _format_datetime, _parse_datetime, _raise_deserialize_error
from workos.common.models.team_production_state import TeamProductionState


@dataclass(slots=True)
class Team:
    """Team model."""

    object: Literal["team"]
    """Distinguishes the team object."""
    id: str
    """Unique identifier of the team."""
    name: str
    """The name of the team."""
    production_state: TeamProductionState
    """Whether the team can host production environments. `Active` means billing is set up. `Inactive` means a team admin must add a payment method in the WorkOS Dashboard. `Suspended` and `Deleting` mean the team can't be provisioned into."""
    production_enabled_at: datetime | None
    """The timestamp when production was enabled for the team, or `null` if it never has been."""
    created_at: datetime
    """The timestamp when the team was created."""
    updated_at: datetime
    """The timestamp when the team was last updated."""

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> Team:
        """Deserialize from a dictionary."""
        try:
            return cls(
                object=data.get("object", "team"),
                id=data["id"],
                name=data["name"],
                production_state=TeamProductionState(data["production_state"]),
                production_enabled_at=_parse_datetime(_v_production_enabled_at)
                if (_v_production_enabled_at := data["production_enabled_at"])
                is not None
                else None,
                created_at=_parse_datetime(data["created_at"]),
                updated_at=_parse_datetime(data["updated_at"]),
            )
        except (KeyError, ValueError) as e:
            _raise_deserialize_error("Team", e)

    def to_dict(self) -> dict[str, Any]:
        """Serialize to a dictionary."""
        result: dict[str, Any] = {}
        result["object"] = self.object
        result["id"] = self.id
        result["name"] = self.name
        result["production_state"] = (
            self.production_state.value
            if isinstance(self.production_state, Enum)
            else self.production_state
        )
        if self.production_enabled_at is not None:
            result["production_enabled_at"] = _format_datetime(
                self.production_enabled_at
            )
        else:
            result["production_enabled_at"] = None
        result["created_at"] = _format_datetime(self.created_at)
        result["updated_at"] = _format_datetime(self.updated_at)
        return result
