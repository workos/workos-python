# This file is auto-generated by oagen. Do not edit.


import pytest
from workos import WorkOS, AsyncWorkOS
from tests.generated_helpers import load_fixture

from workos.user_management.session_tokens.models import JwksResponse
from workos._errors import (
    AuthenticationException,
    NotFoundException,
    RateLimitExceededException,
    ServerException,
)


class TestUserManagementSessionTokens:
    def test_json_web_key_set(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("jwks_response.json"),
        )
        result = workos.user_management.session_tokens.json_web_key_set("test_clientId")
        assert isinstance(result, JwksResponse)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/sso/jwks/test_clientId")

    def test_json_web_key_set_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationException):
            workos.user_management.session_tokens.json_web_key_set("test_clientId")

    def test_json_web_key_set_not_found(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                workos.user_management.session_tokens.json_web_key_set("test_clientId")
        finally:
            workos.close()

    def test_json_web_key_set_rate_limited(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                workos.user_management.session_tokens.json_web_key_set("test_clientId")
        finally:
            workos.close()

    def test_json_web_key_set_server_error(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                workos.user_management.session_tokens.json_web_key_set("test_clientId")
        finally:
            workos.close()


@pytest.mark.asyncio
class TestAsyncUserManagementSessionTokens:
    async def test_json_web_key_set(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("jwks_response.json"))
        result = await async_workos.user_management.session_tokens.json_web_key_set(
            "test_clientId"
        )
        assert isinstance(result, JwksResponse)
        request = httpx_mock.get_request()
        assert request.method == "GET"
        assert request.url.path.endswith("/sso/jwks/test_clientId")

    async def test_json_web_key_set_unauthorized(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationException):
            await async_workos.user_management.session_tokens.json_web_key_set(
                "test_clientId"
            )

    async def test_json_web_key_set_not_found(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                await workos.user_management.session_tokens.json_web_key_set(
                    "test_clientId"
                )
        finally:
            await workos.close()

    async def test_json_web_key_set_rate_limited(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                await workos.user_management.session_tokens.json_web_key_set(
                    "test_clientId"
                )
        finally:
            await workos.close()

    async def test_json_web_key_set_server_error(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                await workos.user_management.session_tokens.json_web_key_set(
                    "test_clientId"
                )
        finally:
            await workos.close()
