# This file is auto-generated by oagen. Do not edit.


import pytest
from workos import WorkOS, AsyncWorkOS
from tests.generated_helpers import load_fixture

from workos.application_client_secrets.models import (
    ApplicationCredentialsListItem,
    NewConnectApplicationSecret,
)
from workos._errors import (
    AuthenticationException,
    NotFoundException,
    RateLimitExceededException,
    ServerException,
)


class TestApplicationClientSecrets:
    def test_list(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=[load_fixture("application_credentials_list_item.json")]
        )
        result = workos.application_client_secrets.list("test_id")
        assert isinstance(result, list)
        assert len(result) == 1
        assert isinstance(result[0], ApplicationCredentialsListItem)

    def test_create(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("new_connect_application_secret.json"),
        )
        result = workos.application_client_secrets.create("test_id")
        assert isinstance(result, NewConnectApplicationSecret)
        assert result.object == "connect_application_secret"
        assert result.id == "secret_01J9Q2Z3X4Y5W6V7U8T9S0R1Q"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/connect/applications/test_id/client_secrets")

    def test_delete(self, workos, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = workos.application_client_secrets.delete("test_id")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/connect/client_secrets/test_id")

    def test_list_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationException):
            workos.application_client_secrets.list("test_id")

    def test_list_not_found(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                workos.application_client_secrets.list("test_id")
        finally:
            workos.close()

    def test_list_rate_limited(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                workos.application_client_secrets.list("test_id")
        finally:
            workos.close()

    def test_list_server_error(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                workos.application_client_secrets.list("test_id")
        finally:
            workos.close()


@pytest.mark.asyncio
class TestAsyncApplicationClientSecrets:
    async def test_list(self, async_workos, httpx_mock):
        httpx_mock.add_response(
            json=[load_fixture("application_credentials_list_item.json")]
        )
        result = await async_workos.application_client_secrets.list("test_id")
        assert isinstance(result, list)
        assert len(result) == 1
        assert isinstance(result[0], ApplicationCredentialsListItem)

    async def test_create(self, async_workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("new_connect_application_secret.json")
        )
        result = await async_workos.application_client_secrets.create("test_id")
        assert isinstance(result, NewConnectApplicationSecret)
        assert result.object == "connect_application_secret"
        assert result.id == "secret_01J9Q2Z3X4Y5W6V7U8T9S0R1Q"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/connect/applications/test_id/client_secrets")

    async def test_delete(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=204)
        result = await async_workos.application_client_secrets.delete("test_id")
        assert result is None
        request = httpx_mock.get_request()
        assert request.method == "DELETE"
        assert request.url.path.endswith("/connect/client_secrets/test_id")

    async def test_list_unauthorized(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationException):
            await async_workos.application_client_secrets.list("test_id")

    async def test_list_not_found(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                await workos.application_client_secrets.list("test_id")
        finally:
            await workos.close()

    async def test_list_rate_limited(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                await workos.application_client_secrets.list("test_id")
        finally:
            await workos.close()

    async def test_list_server_error(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                await workos.application_client_secrets.list("test_id")
        finally:
            await workos.close()
