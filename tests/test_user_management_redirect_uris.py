# This file is auto-generated by oagen. Do not edit.

import json

import pytest
from workos import WorkOS, AsyncWorkOS
from tests.generated_helpers import load_fixture

from workos.user_management.redirect_uris.models import RedirectUri
from workos._errors import (
    AuthenticationException,
    NotFoundException,
    RateLimitExceededException,
    ServerException,
)


class TestUserManagementRedirectUris:
    def test_create(self, workos, httpx_mock):
        httpx_mock.add_response(
            json=load_fixture("redirect_uri.json"),
        )
        result = workos.user_management.redirect_uris.create(uri="test_uri")
        assert isinstance(result, RedirectUri)
        assert result.object == "redirect_uri"
        assert result.id == "ruri_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/redirect_uris")
        body = json.loads(request.content)
        assert body["uri"] == "test_uri"

    def test_create_unauthorized(self, workos, httpx_mock):
        httpx_mock.add_response(
            status_code=401,
            json={"message": "Unauthorized"},
        )
        with pytest.raises(AuthenticationException):
            workos.user_management.redirect_uris.create(uri="test_uri")

    def test_create_not_found(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                workos.user_management.redirect_uris.create(uri="test_uri")
        finally:
            workos.close()

    def test_create_rate_limited(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                workos.user_management.redirect_uris.create(uri="test_uri")
        finally:
            workos.close()

    def test_create_server_error(self, httpx_mock):
        workos = WorkOS(api_key="sk_test_123", client_id="client_test", max_retries=0)
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                workos.user_management.redirect_uris.create(uri="test_uri")
        finally:
            workos.close()


@pytest.mark.asyncio
class TestAsyncUserManagementRedirectUris:
    async def test_create(self, async_workos, httpx_mock):
        httpx_mock.add_response(json=load_fixture("redirect_uri.json"))
        result = await async_workos.user_management.redirect_uris.create(uri="test_uri")
        assert isinstance(result, RedirectUri)
        assert result.object == "redirect_uri"
        assert result.id == "ruri_01EHZNVPK3SFK441A1RGBFSHRT"
        request = httpx_mock.get_request()
        assert request.method == "POST"
        assert request.url.path.endswith("/user_management/redirect_uris")

    async def test_create_unauthorized(self, async_workos, httpx_mock):
        httpx_mock.add_response(status_code=401, json={"message": "Unauthorized"})
        with pytest.raises(AuthenticationException):
            await async_workos.user_management.redirect_uris.create(uri="test_uri")

    async def test_create_not_found(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=404, json={"message": "Not found"})
            with pytest.raises(NotFoundException):
                await workos.user_management.redirect_uris.create(uri="test_uri")
        finally:
            await workos.close()

    async def test_create_rate_limited(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(
                status_code=429,
                headers={"Retry-After": "0"},
                json={"message": "Slow down"},
            )
            with pytest.raises(RateLimitExceededException):
                await workos.user_management.redirect_uris.create(uri="test_uri")
        finally:
            await workos.close()

    async def test_create_server_error(self, httpx_mock):
        workos = AsyncWorkOS(
            api_key="sk_test_123", client_id="client_test", max_retries=0
        )
        try:
            httpx_mock.add_response(status_code=500, json={"message": "Server error"})
            with pytest.raises(ServerException):
                await workos.user_management.redirect_uris.create(uri="test_uri")
        finally:
            await workos.close()
