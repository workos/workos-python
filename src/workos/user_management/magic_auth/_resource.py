# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any, Dict, Optional

if TYPE_CHECKING:
    from ..._client import AsyncWorkOSClient, WorkOSClient

from .models import MagicAuth
from ..._types import RequestOptions


class UserManagementMagicAuth:
    """User Management Magic Auth API resources."""

    def __init__(self, client: "WorkOSClient") -> None:
        self._client = client

    def send_magic_auth_code_and_return(
        self,
        *,
        email: str,
        invitation_token: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> MagicAuth:
        """Create a Magic Auth code

        Creates a one-time authentication code that can be sent to the user's email address. The code expires in 10 minutes. To verify the code, [authenticate the user with Magic Auth](https://workos.com/docs/reference/authkit/authentication/magic-auth).

                Args:
                    email: The email address to send the magic code to.
                    invitation_token: The invitation token to associate with this magic code.
                    request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

                Returns:
                    MagicAuth

                Raises:
                    BadRequestException: If the request is malformed (400).
                    UnprocessableEntityException: If the request data is unprocessable (422).
                    RateLimitExceededException: If rate limited (429).
                    AuthenticationException: If the API key is invalid (401).
                    ServerException: If the server returns a 5xx error.
        """
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "email": email,
                "invitation_token": invitation_token,
            }.items()
            if v is not None
        }
        return self._client.request(
            method="post",
            path="user_management/magic_auth",
            body=body,
            model=MagicAuth,
            request_options=request_options,
        )

    def get(
        self,
        id: str,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> MagicAuth:
        """Get Magic Auth code details

        Get the details of an existing [Magic Auth](https://workos.com/docs/reference/authkit/magic-auth) code that can be used to send an email to a user for authentication.

                Args:
                    id: The unique ID of the Magic Auth code.
                    request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

                Returns:
                    MagicAuth

                Raises:
                    NotFoundException: If the resource is not found (404).
                    AuthenticationException: If the API key is invalid (401).
                    RateLimitExceededException: If rate limited (429).
                    ServerException: If the server returns a 5xx error.
        """
        return self._client.request(
            method="get",
            path=f"user_management/magic_auth/{id}",
            model=MagicAuth,
            request_options=request_options,
        )


class AsyncUserManagementMagicAuth:
    """User Management Magic Auth API resources (async)."""

    def __init__(self, client: "AsyncWorkOSClient") -> None:
        self._client = client

    async def send_magic_auth_code_and_return(
        self,
        *,
        email: str,
        invitation_token: Optional[str] = None,
        request_options: Optional[RequestOptions] = None,
    ) -> MagicAuth:
        """Create a Magic Auth code

        Creates a one-time authentication code that can be sent to the user's email address. The code expires in 10 minutes. To verify the code, [authenticate the user with Magic Auth](https://workos.com/docs/reference/authkit/authentication/magic-auth).

                Args:
                    email: The email address to send the magic code to.
                    invitation_token: The invitation token to associate with this magic code.
                    request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

                Returns:
                    MagicAuth

                Raises:
                    BadRequestException: If the request is malformed (400).
                    UnprocessableEntityException: If the request data is unprocessable (422).
                    RateLimitExceededException: If rate limited (429).
                    AuthenticationException: If the API key is invalid (401).
                    ServerException: If the server returns a 5xx error.
        """
        body: Dict[str, Any] = {
            k: v
            for k, v in {
                "email": email,
                "invitation_token": invitation_token,
            }.items()
            if v is not None
        }
        return await self._client.request(
            method="post",
            path="user_management/magic_auth",
            body=body,
            model=MagicAuth,
            request_options=request_options,
        )

    async def get(
        self,
        id: str,
        *,
        request_options: Optional[RequestOptions] = None,
    ) -> MagicAuth:
        """Get Magic Auth code details

        Get the details of an existing [Magic Auth](https://workos.com/docs/reference/authkit/magic-auth) code that can be used to send an email to a user for authentication.

                Args:
                    id: The unique ID of the Magic Auth code.
                    request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

                Returns:
                    MagicAuth

                Raises:
                    NotFoundException: If the resource is not found (404).
                    AuthenticationException: If the API key is invalid (401).
                    RateLimitExceededException: If rate limited (429).
                    ServerException: If the server returns a 5xx error.
        """
        return await self._client.request(
            method="get",
            path=f"user_management/magic_auth/{id}",
            model=MagicAuth,
            request_options=request_options,
        )
