# This file is auto-generated by oagen. Do not edit.

from __future__ import annotations

from typing import TYPE_CHECKING, Any

if TYPE_CHECKING:
    from .._client import AsyncWorkOSClient, WorkOSClient

from .._types import RequestOptions
from .models import Team


class PlatformTeams:
    """Platform Teams API resources."""

    def __init__(self, client: WorkOSClient) -> None:
        self._client = client

    def create_team(
        self,
        *,
        admin_email: str,
        name: str,
        request_options: RequestOptions | None = None,
    ) -> Team:
        """Create a team

        Creates a team along with its default project, a staging environment, and a production environment. An admin invitation is sent to `admin_email`, onboarding is marked complete with AuthKit enabled, and the calling platform is authorized to act inside the team.

        Args:
            admin_email: The email address of the person who will administer the team. An invitation is sent to this address.
            name: Name of the team.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Team

        Raises:
            ConflictError: If a conflict occurs (409).
            UnprocessableEntityError: If the request data is unprocessable (422).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        body: dict[str, Any] = {
            "admin_email": admin_email,
            "name": name,
        }
        return self._client.request(
            method="post",
            path=("platform", "teams"),
            body=body,
            model=Team,
            request_options=request_options,
        )

    def get_team(
        self,
        team_id: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> Team:
        """Get a team

        Returns a team, and doubles as a health check on the platform's access to it. Read `production_state` before attempting to create a production environment.

        Args:
            team_id: The ID of the team.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Team

        Raises:
            NotFoundError: If the resource is not found (404).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        return self._client.request(
            method="get",
            path=("platform", "teams", str(team_id)),
            model=Team,
            request_options=request_options,
        )


class AsyncPlatformTeams:
    """Platform Teams API resources (async)."""

    def __init__(self, client: AsyncWorkOSClient) -> None:
        self._client = client

    async def create_team(
        self,
        *,
        admin_email: str,
        name: str,
        request_options: RequestOptions | None = None,
    ) -> Team:
        """Create a team

        Creates a team along with its default project, a staging environment, and a production environment. An admin invitation is sent to `admin_email`, onboarding is marked complete with AuthKit enabled, and the calling platform is authorized to act inside the team.

        Args:
            admin_email: The email address of the person who will administer the team. An invitation is sent to this address.
            name: Name of the team.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Team

        Raises:
            ConflictError: If a conflict occurs (409).
            UnprocessableEntityError: If the request data is unprocessable (422).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        body: dict[str, Any] = {
            "admin_email": admin_email,
            "name": name,
        }
        return await self._client.request(
            method="post",
            path=("platform", "teams"),
            body=body,
            model=Team,
            request_options=request_options,
        )

    async def get_team(
        self,
        team_id: str,
        *,
        request_options: RequestOptions | None = None,
    ) -> Team:
        """Get a team

        Returns a team, and doubles as a health check on the platform's access to it. Read `production_state` before attempting to create a production environment.

        Args:
            team_id: The ID of the team.
            request_options: Per-request options. Supports extra_headers, timeout, max_retries, and base_url override.

        Returns:
            Team

        Raises:
            NotFoundError: If the resource is not found (404).
            AuthenticationError: If the API key is invalid (401).
            RateLimitExceededError: If rate limited (429).
            ServerError: If the server returns a 5xx error.
        """
        return await self._client.request(
            method="get",
            path=("platform", "teams", str(team_id)),
            model=Team,
            request_options=request_options,
        )
